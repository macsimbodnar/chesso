# Testing ledger

Acceptance criteria with their covering tests. Rows are added together with the
feature, never afterwards. A step cannot complete without a row referencing its
id. Appends only, with one pruning exception (2026-08-13): when the checker
prunes a completed step's plan.md entry, every ledger row all of whose step ids
were pruned in that pass leaves with it.

Two things about that, both corrected at S064 after being stated wrongly here
since 2026-08-13. **The checker keeps the last five completed entries in
plan.md's list order, not the five most recently completed** — retention is a
window over positions, so an entry low in the list outlives newer completions
(`prune_plan()` and `PLAN_DONE_KEPT = 5` in the moltke plugin at **version
0.11.0**, which is not in this tree; the symbols are the citation, the line
numbers `bin/moltke.py:1698-1700` and `:1681` only held at that version). And
**a row is protected only by naming a step id that was not dropped in that
pass**: the
checker matches `S<nnn>` and nothing else, so naming an invariant or a decision
protects nothing, and 17 such rows have been pruned — `| S037 | the reporting
change leaves the tree identical (INV-6) |` among them. A row naming no step id
at all is never pruned, which is why the `INV-1` to `INV-6` rows below stay.

`plan_done/` keeps the step's stamp and git keeps every pruned row.

Two kinds of row appear here, and the difference matters. A **test** row names
something in `ctest` that fails on regression. A **measurement** row names a run
that produced a number at a point in time; it does not re-run, and it is
evidence rather than a guard. INV-6 is why measurement rows exist at all: a
change that alters play is retained only against a verdict, and a verdict of
zero is recorded as zero.

Rows S001 to S016 were written retrospectively when moltke was adopted
(DEC-017). Their measurements are transcribed from the commits and the plan
documents they replace; their test columns name tests that exist today.

| Step | Criterion | Covering test | Result |
|---|---|---|---|
| INV-1 | legal-only generation, perft exact against an oracle | `test_movegen` "shallow perft matches every column"; `test_perft` (label `slow`); `bench_movegen` verifies counts before printing | green |
| INV-2 | make/unmake are exact inverses | `test_engine` "hash and board survive make/unmake"; debug build asserts `squares[]` against the bitboards on every make and unmake | green |
| INV-3 | captures and quiets partition the full list | `test_movegen` "captures and quiets partition the list", 90.5 M assertions over a three-ply tree from every test FEN | green |
| INV-4 | accumulators equal a full recomputation | `assert(eval_accumulators_match(...))` at three sites in `src/bitboard.cpp`, debug build only | green |
| INV-5 | `evaluate()` is side-to-move relative | `test_evaluation` "colour symmetry over every test position", "a mirrored start position is balanced" | green |
| INV-6 | behaviour-neutral changes prove it | `tools/search_bench.py` node counts and best moves, compared by hand per change | procedure, not a test |
| S015 | quiescence pruning measured in games | fastchess | measured, **0 Elo**, kept; rerun outstanding, folded into S022 |
| S018 | the phase that costs most is named by measurement | early middlegame 44.1 cp/move and 36.0 % of loss; endgame 18.3 cp/move and 20.5 % | measured, **contradicts the S016 anecdote**, S019 affected |
| DEC-033 | 16x more search removes only a quarter of the error | `tools/depth_vs_eval.py` over 160 positions: 170.1 cp/move at 4M nodes, 129.1 at 64M, 95 of 160 moves unchanged; evidence in `adocs/data/DEC033_depth_vs_eval.tsv` | measured, **evaluation-limited**, plan reordered |
| DEC-035 | the engine is still evaluation-limited after the fit | `tools/depth_vs_eval.py`, same criteria and sample size as DEC-033: 29.8 % of the error removed by 16x search against 24.1 %, 78 of 160 moves unchanged against 95, 10.4 cp per doubling against 10.3 | measured, **shifted toward search, order not turned over** |
| DEC-035 | the fitted evaluation is no longer systematically optimistic | same profile: mean bias -1.5 / -24.3 / -40.3 / -3.5 / +204.3 by phase, against +39 / +77 / +100 / +42 / +30 before; medians 6 / 0 / -4 / -1 / 227 | measured, **reversed in four phases of five**, MANUAL.md rewritten |
| S028 | the gain shows against a fixed opponent, not only against the previous commit | 120 games achesso vs sgambetto at 10+0.2 under the loose adjudication of DEC-031: 67.5 %, +126.97 +/- 60.30 Elo, against 39.3 % over 210 games under identical settings at S018 | measured, **chained SPRT gains are not drift** |
| DEC-036 | an evaluation term can be priced without the search confounding it | `tests/bench_eval`, ten positions from a full board to bare kings, scores and checksum printed before any timing; 40000000 calls a sweep, resolution 0.1 % over three interleaved passes | green, instrument |
| DEC-036 | recomputed mobility costs what INV-4 exists to prevent | `bench_eval`: 1.31 ns per call against 15.93, 12.2x. `search_bench` depth 12: nps -32.8 % kiwipete, -45.5 % midgame, -23.7 % tactical, wall time +33 % | measured, **probe reverted, nothing shipped** |
| DEC-037 | recomputed mobility is decided by play, not by nodes per second | `REF=c8fe860 ./fastchess.sh --fast`, 10+0.2: **-14.93 +/- 16.44 Elo** over 1164 games, LLR -2.23 crossing -2.20, H0 accepted, LOS 3.72 %, Ptnml [77, 129, 203, 113, 60] | measured, **rejected and reverted**, weights were never fitted |
| S034 | the margin is chosen from data, not from a guess | tapered mobility over 149084 S028 self-play positions: p50 19, p75 34, p90 50, p95 60, p99 81, p99.9 107, max 143 | measured, margin set to 150 |
| S027 | what the fitted weights do to the margin, measured rather than assumed | `build/tools/eval_spread --data .tuning/selfplay_v1.tsv --limit 200000`: combined correction p99 129, p99.9 179, max 289; king safety alone p99 54, max 143; over `LAZY_EVAL_MARGIN` on 797 of 200000 positions, 0.399 %, against mobility's own 231 and 0.116 % | measured, no verdict: the clamp keeps the shortcut sound either way, and S034's margin was chosen from mobility's distribution alone |
| S027 | the tuner emits every parameter it fits, three terms later | 40-epoch smoke run over 1490839 rows writes `pawn_structure_mg` and `pawn_structure_eg` into the pasteable header alongside the other six arrays | green; the writer was extended in the same commit as the model, which is what S034 did not do and cost eight discarded weights |
| S027 | the tuner emits every parameter it fits, four terms later | 40-epoch smoke run over 1490839 rows writes `piece_placement_mg` and `piece_placement_eg` into the pasteable header alongside the other eight arrays | green; the writer was extended in the same commit as the model, which is what S034 did not do and cost eight discarded weights |
| S027 | the tuner emits every parameter it fits, five terms later | 40-epoch smoke run over 1490839 rows writes `tempo_mg` and `tempo_eg` into the pasteable header alongside the other ten arrays | green; the writer was extended in the same commit as the model, which is what S034 did not do and cost eight discarded weights |
| S044 | the owner-run boundary reads DEC-015 as amended by DEC-041 everywhere it is stated | `grep -n "DEC-015\|owner" adocs/plan.md adocs/specs.md` — every workflow hit assigns the owner only S029's network training; procedure, not a test | checked; before the step both files assigned fits and tuning to the owner |
| S045 | S024 scopes continuation history as two tables with a verdict each, distinct from the countermove heuristic | `grep -n "countermove\|one-ply\|two SPRT" adocs/plan_todo/S024_continuation_history.md` — the note names the heuristic as present, history as absent, and the accepts asks one verdict per table; procedure, not a test | checked; before the step the note called the countermove table one-ply continuation history |
| S046 | S030, S031 and S032 each carry an INV-6 discharge in their accepts | `grep -n "search_bench\|INV-6" adocs/plan_todo/S030_move_encoding_16_bit.md adocs/plan_todo/S031_single_side_to_move_key.md adocs/plan_todo/S032_pext_sliding_attacks.md` — all three accepts name search_bench identity, S030 carries the SPRT branch, S031 states the xor construction that makes hashes bit-identical; procedure, not a test | checked; before the step all three stopped at perft, which says nothing about the tree |
| S047 | no pending step or spec line still describes the retired Apple machine as current | `grep -n "cannot be measured here\|Blocked on hardware\|opendirectoryd\|stops being optional" adocs/plan_todo/*.md adocs/specs.md` — the only hit is specs.md's dated 2026-08-13 note recording the replaced text; the 12 % is_check figure carries its Apple condition; procedure, not a test | checked; before the step S032 read blocked, S029 called x86 non-optional, specs priced SPRTs on three Apple cores |
| S049 | S039 cites the decision that governs the lazy shortcut | `grep -n "DEC-" adocs/plan_todo/S039_lazy_margin_redecide.md` — decisions reads DEC-039, DEC-034 appears nowhere; procedure, not a test | checked; before the step it cited the one-night fit delegation, a DEC-034/S034 transposition |
| S050 | S022 asks one verdict per change, and its accepts and body agree | `grep -n "two SPRT verdicts\|one at a time" adocs/plan_todo/S022_delta_pruning_quiescence.md` — the accepts asks two verdicts in either order, the body asks one at a time, no line asks one run to measure two changes; procedure, not a test | checked; before the step the accepts required "the same run" to re-measure S015, which its own body forbade |
| S051 | S020 and S030 complete on a measurement, not on a positive gain | `grep -n "zero is recorded as zero" adocs/plan_todo/S020_single_check_computation.md adocs/plan_todo/S030_move_encoding_16_bit.md` — both gates ask for a number with its noise floor and a keep-or-revert call, S020 names hyperfine over interleaved fixed-depth runs; procedure, not a test | checked; before the step both required a gain larger than the resolution, which the modal honest outcome (DEC-019: 0, 0, slower) cannot satisfy |
| S052 | S021 points at the file where iterative deepening lives | `grep -n "iterative" src/*.cpp adocs/plan_todo/S021_aspiration_windows.md` — the loop is src/chesso.cpp:544 and the touches line says so, agreeing with S037; procedure, not a test | checked; before the step it named src/search.cpp, disagreeing with S037 about the same loop |
| S040 | the section's step attributions name the steps that added the parameters | mobility at S034 from `adocs/plan_done/S034_evaluation_call_cost.md:3,197`; king safety, passed pawns, pawn structure, piece placement and tempo at S027 from `adocs/plan_done/S027_handcrafted_eval_terms.md:2,163,172` and `adocs/specs.md:72`; procedure, not a test | checked, **unchanged**: `tools/eval_model.hpp:52`, `tools/tuner.cpp:8`, `:628` and `:669` date passed pawns to S035, which is `S035_fastchess_script_repair` and has nothing to do with the evaluation — the plan record wins on what a step id names, and the four code comments are a defect outside this step's `excludes:` |
| S041 | the S040 rows that cite `free_mask` still point at the file it lives in | S041's move invalidated three coordinates S040 recorded one commit earlier: `GROUP_LIST` and the four `free_mask` comments are `tools/tuner_groups.hpp:37-39` and `:70-72`, `:76-78`, `:82-84`, `:88-90`, not `tools/tuner.cpp:153-155` and `:186-189`, `:192-194`, `:198-200`, `:204-206`; `tempo` is `:93-95`, not `:209-211`; and removing the 71-line region shifted the rest of `tuner.cpp` up by 66, so `write_tables()` is `:524-695`, not `:590-761`. The rows above are dated evidence and are not rewritten; this row is where the new coordinates live; procedure, not a test | checked, **relocated**; rows 61 to 63 would otherwise send a reader to a file that no longer contains the symbol |
| S038 | the guard is made honest without altering play, so no SPRT is owed | `git diff --quiet 7068cd2 -- src/evaluation.cpp tools/eval_model.hpp` exits 0, and `sha256sum` agrees either side: `18ab9550…5323` for `src/evaluation.cpp`, `b3a96b65…5335` for `tools/eval_model.hpp`. Only `tests/test_eval_model.cpp` changed, so `evaluate()` is bit-identical and INV-6 has nothing to discharge; procedure, not a test | checked, **identical**; the one-division merge that would alter play is S055, after S042 and before S029, with its verdict expected at 0 (DEC-053) |
| DEC-055 | `--allow-tactical` is inert at its default, so the S028 filter stays exactly reachable | `build/tools/datagen --games 240 --nodes 100000 --threads 12 --seed 20260814` through the pre-change binary and the post-change binary. Both print `240 games, 17593 positions`, and `sort \| md5sum` of the two outputs is `2a13141f0b86f333684df5b2be333104` either way. Sorting is required and is not a weakening: workers append under one mutex per finished game, so the row *order* across threads is not deterministic between runs while the row *set* is; procedure, not a test | measured, **identical**; the flag defaults to 0 and the counters read the same booleans the filter already computed |
| DEC-055 | each datagen filter clause has a measured marginal cost, not an argument | `tools/datagen.cpp` prints one line per run counting, per clause, the positions that would have been recorded but for that clause alone. 240 games at seed 20260814, defaults: `27839 considered, 17593 recorded; skipped in-check 1698, tactical best move 4814, mate 0, score past 1000 1824`. The tactical clause is the largest at 17.3 % of considered, +27.4 % on top of what is recorded | measured, **the loosened clause is the one the code contradicts**: `src/search.cpp:125` calls `evaluate_lazy()` at the top of every quiescence node, before `generate_captures` at `:152`, and returns on that stand-pat score at `:136-137` |
| DEC-055 | the score cap is kept on a measurement, not on taste | same 240 games at `--quiet-limit 1000000 --allow-tactical 1`: 24603 rows, 2196 of them (8.93 %) past 1000. Weighting each row by `sig * (1 - sig)` as `tools/tuner.cpp:380-384` does, at the K this corpus fits (0.7472), those rows carry **0.206 %** of the corpus's gradient mass; the factor is 0.01319 at a score of 1000 against 0.25 at an equal position, 5.28 % | measured, **near-inert, cap kept at 1000**; the same run shows the mate clause is shadowed rather than redundant — 0 marginal rejections under the cap, 649 without it |
| S065 | the eight-hour corpus size is extrapolated from a measured rate, not guessed | 600 games at `--nodes 100000 --threads 12 --allow-tactical 1`: 56304 positions in 143.93 s. 4.169 games/s and 391.2 positions/s, so 28800 s gives 120067 games and 11.27 M positions; 120000 x 93.84 positions per game = 11.26 M cross-checks it. 64.15 B/row = 723 MB against 34 G free; the tuner's 10.9 MB resident at 56304 rows scales to about 2.2 GB against 9 GB available, and 2.22 ms/epoch to about 0.44 s/epoch, so S028's 11200 epochs cost roughly 1 h 23 m | measured, **120000 games**; S028's corpus was 1490839 positions from 20000 games, so 7.6x the rows and 6.0x the independent games |
| S066 | nearly every game lands on both sides of the tuner's validation split | `tools/tuner.cpp:795-805` shuffles a **row** index and takes the last 10 %; `tools/datagen.cpp` writes a game's rows inside one `lock_guard`, so they are contiguous. Over the 56304-row smoke corpus, descents in the FEN's fullmove number give **600 contiguous runs for 600 games**, one per game exactly. Simulating the split over the real per-game row counts: 599, 599 and 597 of 600 games straddle it — **99.5 to 99.8 %**. Analytically `0.9^93.84` = 5.1e-05, and 3.9e-04 at S028's 74.5 rows per game | measured, **the held-out set is not held out**; it changes a diagnostic and no game, which is why S066 is minutes and sits ahead of S065's fit rather than behind it |
| S066 | the S065 corpus fitted under both splitters, held-out figures side by side | `.tuning/selfplay_v2.tsv`, 11003693 rows, `--k 0.7615` pinned to the whole-corpus fit so the objective is identical on both sides, `--seed 1 --epochs 3000 --report 100 --patience 1000000 --threads 12`. Row level: 9903324 train, 1100369 held out, start 0.122699 / 0.122933, best **train 0.117053 validation 0.117352**. Game level: 119998 games, 9903305 train, 1100388 held out, start 0.122741 / 0.122560, best **train 0.117059 validation 0.117380**. Held-out difference **+0.000028**; generalisation gap (held out minus train) +0.000299 against +0.000321, widened by 0.000022 | measured, **no difference that can be attributed**, recorded as zero: the two held-out figures differ by 2.8e-05 while the two held-out **sets** already differ by 3.7e-04 at the untuned starting constants, thirteen times as much. 827 parameters over 9.9 M rows cannot memorise a game. The splitter is kept because the old instrument was wrong by construction, not because a number moved |
| S065 | a fit over the regenerated corpus reduces held-out error below the constants the engine ships, with no refusal warning | `build/tools/tuner --data .tuning/selfplay_v2.tsv --out .tuning/tuned_v2.hpp --threads 12 --epochs 5000`, everything else default (`--lr 1.0 --report 100 --patience 20 --validation 0.1 --seed 1`): 11003693 positions, 119998 games, 9903305 train, 1100388 held out (10.0002 %), 827 parameters all free, **K = 0.7624** fitted from the train rows and then held. Start train 0.122741 / held out 0.122560; best **train 0.117043 / held out 0.117359, at epoch 4800**, no refusal warning, 2082 s wall on 12 threads. Curve: 0.118022 at 100 epochs, 0.117532 at 1000, 0.117424 at 2000, 0.117380 at 3000, 0.117366 at 4000, 0.117359 at 4800 — 87.3 % of the total gain is inside the first 100 epochs and 96.7 % inside the first 1000. Emitted header sha256 `b60e7134…40bc4` | measured, **descends by 4.24 % of the starting held-out error**; **not comparable with S028's 0.113852 → 0.108043** — different corpus, different objective, K refitted per corpus (0.7624 against 1.1141), and S028's figures were taken under the row-level split S066 replaced. Only the SPRT decides, and it has not run |
| S065 | applying the fitted constants fires three guards, each with the arithmetic that decides it | `cmake --build build -j12 && ctest --test-dir build -L fast` at the paste: **9 of 12 green, 3 red**, `test_evaluation`, `test_search`, `test_eval_model`. (1) `tempo_mg = 39, tempo_eg = 21`, so `test_eval_model`'s `CHECK(tempo_unfitted)` fails with its own message: all four taperings can truncate, the bound is 4 x 23/24 = **3.833** and the tolerance has to go to 4 — DEC-053's stated consequence, arrived at by fitting tempo for the first time. (2) the same case's four pinned FENs, whose residuals belong to the old weights, now differ by **2.541667 / 1.250000 / 1.125000 / 2.416667** against 2.875 / 2.333333 / 2.25 / 2.125, so two fall under `> 2.0` and the worst under `> 2.8`. (3) `test_evaluation` "removing a piece moves the score": a white queen on d1 against bare kings is worth **713** where her own fitted material value is 1152, off by **439** against `POSITIONAL_ROOM` 150 — decomposed as -354 from `psqt_eg[queen][d1]` at phase 4, -116 from `mobility_eg[queen]` over 17 squares, +28 king safety | measured, **the paste is reverted and no SPRT was run**. (1) is arithmetic DEC-053 already decided; (2) and (3) are guards whose thresholds are evidence of the old weights, and re-targeting them is a decision rather than this step's business. S056 does **not** unblock it: its `touches:` is `adocs/plan_todo/S055_taper_stage_two_once.md` and its `excludes:` forbids editing `tests/test_eval_model.cpp` |
| S065 | the value anchors the paste moves are re-derived independently of the engine rather than read off it | a second implementation of `evaluate()` over seven positions, written from `src/evaluation.cpp`'s specification — the four tapered divisions truncating towards zero at `:640`, `:668`, `:951`, `:953`, the `LAZY_EVAL_MARGIN` clamp at `:984`, the side-to-move sign at `:672` — with every feature count hand-derived from the placement. Against the weights the engine ships it reproduces **8 of 8** pinned values: `test_evaluation`'s 88, 240, 325, 530, 1101, 0 and `test_search`'s 530 with its cheap 524 and its 266. Against the fitted header the same seven positions give 148, 237, 283, 515 (cheap 536), 734, **21** and 251 — every one of them what the engine printed at the paste. 21 is the bare-kings case: the board terms still cancel and what is left is tempo at phase 0 | measured, **agrees on both weight sets**; an anchor copied from the thing it anchors asserts nothing (S028). `test_search`'s two -491 cases moved to -454 (a quiescence result) and -503 (a static evaluation after a played sequence) and were **not** re-derived here |
| S065 | the tuner can hold named groups at what the engine ships while fitting everything else | `test_tuner_groups` "--freeze clears exactly the named groups" and "--freeze takes a list, and refuses what it cannot honour": every group in `GROUP_LIST` frozen out of `all` one at a time and asserted to be the exact complement of its own `--only` range, then `tempo,piece_placement` together giving **817 free of 827** with every index checked individually. Order and spacing immaterial (`"piece_placement, tempo"` gives the identical mask), an empty list a no-op against `all`, and four refusals: `not_a_group`, `tempo,not_a_group`, `all`, and — in `main()` — an `--only`/`--freeze` pair leaving nothing free. Preconditions asserted first: each group non-empty, and `tempo` and `piece_placement` non-empty and disjoint | green, 4 cases, 1757 assertions, 0.00 s, label `fast`; observed red with `freeze_mask` stubbed to `return true`, **2 of 4 cases failing on 22 assertions**, verbatim `CHECK( 1 == 0 )` at every index from 819 to 826 and `CHECK_FALSE( true )` on all three refusals. The three partition properties S041 placed are untouched — `freeze_mask` selects among the ranges `free_mask` already defines and changes no group boundary |
| S067 | the move-ordering budget is set from a measured count with the margin stated | `test_search` "ordering keeps the tree small": `state.node_limit` 1000000 -> **440000**, and a new lower bound `REQUIRE(result.explored_nodes > 20000)` so the budget cannot quietly stop bounding anything. Measured rather than guessed: `CHECK(result.explored_nodes == 0)` in this case reported `CHECK( 109575 == 0 )`, cold table, depth 5, `TRICKY_POS`. 440000 is 4.0x that, against the 9.1x the old budget allowed | green; observed red both ways. Budget at 50000: `FATAL ERROR: REQUIRE_FALSE( state.aborted ) is NOT correct!  values: REQUIRE_FALSE( true )`. Lower bound at 200000: `REQUIRE( result.explored_nodes > 200000 ) is NOT correct!  values: REQUIRE( 109575 >  200000 )` / `logged: depth 5 on TRICKY_POS cost 109575 nodes; the 440000 budget was set from 109575 and no longer bounds anything - re-measure it`. The margin is 4x rather than tighter because the evaluation constants order the moves and S065 is a pending 827-constant paste |
| S062 | no sentence in `plan.md`'s prose describes a completed step as pending or as next | Mechanical and re-runnable, `tools/plan_prose_check.py`: every `S\d{3}` outside the ordered list is resolved against `plan_done/`, `plan_current/` and `plan_todo/`, then each **sentence** naming a completed id is tested for a pending marker — `is next`, `still pending`, `everything pending`, `at the front of the pending`, `the last of the three instruments`, `pending order`, `will be`, `is planned`. **46 ids resolved in prose, 25 of them completed.** Sentence-wise and not line-wise on purpose: the prose is hard-wrapped, and the first version of this check was line-based and **missed two of the five** because the id and the claim about it sat on different lines | green, **0 sentences flagged**, exit 0. **Observed red on the same check against `git show HEAD:adocs/plan.md`: 5 sentences flagged**, two more than `2026-08-13_plan_review.2-F07` recorded — S037 as "the last of the three instruments still pending", S044 to S052 as "ahead of everything still pending", S054 as "first of everything pending", **S065 as "at the front of the pending order"** which the finding predates, and **S033 as "next in this order"** which this session's own completion created. That last one is the recurrence the step file predicted: `2026-08-13_plan_review-F05` was the same class, S048 cleared it, and it returned four completions later |
| S064 | `plan.md` and `testing.md` state the checker's retention as it behaves, not as it reads | Both claims traced to the moltke plugin at the version in use, **0.11.0**, which is not in this tree (`bin/moltke.py` there, and the line numbers below hold at that version only): `PLAN_DONE_KEPT = 5` at `:1681`, and `prune_plan()` at `:1698-1700` collects completed entries **by position in the file** and drops `entry_lines[:-PLAN_DONE_KEPT]`, so what survives is the last five in list order. The ledger predicate at `:1717-1721` is `ids <= dropped_ids` over `re.findall(r"\bS\d{3}\b", line)`, so a row leaves only when every step id it names went in that same pass, and no other token is consulted | measured against the tree, not read off the docs. **The window when this step was written held S067, S065, S033, S062 and S053**, and completing S064 evicted S067 in front of the change — the window after it is S065, S033, S062, S064, S053. S053 completed at **18:42 on 2026-08-13** and is in both, while S037 (19:13), S043 (19:29), S066 and now S067 all completed later and are pruned: S053 survives on position, being the last line of the list, and every one of those four sat above it. The second correction is demonstrated from history: `git log -p --follow -- adocs/testing.md` shows **17 pruned rows naming an `INV-n` or a `DEC-n`**, including `\| S037 \| the reporting change leaves the tree identical (INV-6) \|`, so the header's claim that naming an invariant or a decision protects a row was false. Rows naming no step id are never pruned, which is what actually keeps `INV-1` to `INV-6` in place |
| S069 | `status.md`'s parked list agrees with `specs.md` about the machine, and its retention item states the mechanism instead of a census | Both claims traced to the files they copy from. The machine item now carries `specs.md:189-194`'s figures — three to four and a half hours, DEC-048/DEC-050 settings, all 12 threads of the DEC-049 machine, `8moves_v3.pgn` — and cites that range; the retention item cites `plan.md:173-187`, verified to be the paragraph stating `PLAN_DONE_KEPT` retention by list position against `prune_plan()` in moltke 0.11.0 (`bin/moltke.py:1698-1700` there; the range was `:173-182` before S071 grew the paragraph), and `adocs/plan_todo/S068_rfp_margin_retune.md:48` is the "44 m 10 s at 12 cores for 1012 games" line. Mechanical check: `grep -n "opendirectoryd\|x86-64\|three usable cores\|about an hour\|position 66\|38 to 41" adocs/status.md`; procedure, not a test | green, **0 lines matched**. Observed red against `git show HEAD:adocs/status.md`: **5 lines**, `:24-25` the stale census and `:62-64` the macOS pricing. The census was wrong in every number, which is why it is not replaced with a fresher one — completing this step pruned another entry and shifted the labels again, on the same turn. The durable half is structural and stated instead: **S038, S066 and S067 have no list entry at all**, so "S038, S066, S067 and S065 sit at 38 to 41" named three positions that do not exist, and S053 is the last line of the list, which is why it outlives everything above it. `moltke --validate` clean; `ctest --test-dir build -L fast` **12 of 12, 0 failed, 21.40 s**. `README.md` owner-written, no change needed; `MANUAL.md` checked, nothing playing or surfacing changed |
| S071 | `plan.md` and `testing.md` cite the checker by version and symbol, not by a path this tree does not have | Every citation now names **moltke 0.11.0** and the two symbols it rests on, `prune_plan()` and `PLAN_DONE_KEPT`, and says the file is not in this tree; line numbers are kept but demoted to "at that version". Verified against `~/.claude/plugins/cache/moltke/moltke/0.11.0/bin/moltke.py`: `PLAN_DONE_KEPT = 5` at `:1681`, `def prune_plan(root, done_ids)` at `:1684`, the entry collection at `:1698-1699` and `drop = set(entry_lines[:-PLAN_DONE_KEPT])` at `:1700`, the ledger predicate `ids <= dropped_ids` at `:1717-1721`. Mechanical check, paragraph-scoped over both files: every block containing `moltke.py` also contains `0.11.0` or `not in this repository`; procedure, not a test | green, **0 bad blocks**. Observed red at HEAD via `git stash`: **3 bad blocks** — `plan.md`'s retention paragraph, `testing.md`'s header, and the ledger table. The version is load-bearing and that is measured, not asserted: **0.1.0 is also installed** on this machine (`~/.claude/plugins/cache/moltke/moltke/0.1.0/bin/moltke.py`, 991 lines against 0.11.0's 2624) and has **neither `prune_plan` nor `PLAN_DONE_KEPT`** — `grep -n` returns nothing for both — so the old bare-path citation was not merely unfollowable, it described behaviour that does not exist under the other installed version. `installed_plugins.json` records 0.11.0 as the active install. Claims themselves unchanged. One consequence outside `touches:`: growing `plan.md`'s paragraph moved its end from `:182` to `:187`, so S069's citation of it in `status.md:25` and in its own ledger row was corrected in the same commit rather than left stale. `moltke --validate` clean; `cmake --build build -j12 && ctest -L fast` **12 of 12, 0 failed, 20.07 s**; `./clang-format.sh --check` exit 0. `README.md` owner-written, no change needed; `MANUAL.md` checked, no surface or play change |
| S072 | S068's sweep evidence is a tracked file and its two floors are told apart | Nine files copied to `adocs/data/` under an `S033_` prefix (three `.tsv`, three `.sh`, three `.log`), sha256 matched against the surviving scratchpad copy before the copy. Mechanical check, `adocs/plan_todo/S068_rfp_margin_retune.md`: `grep -c scratchpad` = 0, and `grep -o 'adocs/data/S033_rfp_[a-z_]*\.tsv' \| sort -u \| wc -l` = 3. Column claim traced to the data, not to prose: at margin 75, `max` 6, the floors run 1190649 / 2142924 / 2886952 for `min` and 1190649 / 1203441 / 1216123 for `min_ply`, and they coincide to the node at floor 1 — where both mean "exempt nothing" — which is what says the two tables are one code base measured twice; procedure, not a test | green, **0 scratchpad citations, 3 of 3 tables cited**. Observed red against `git show HEAD:adocs/plan_todo/S068_rfp_margin_retune.md`: **2 scratchpad citations, 0 tracked**, and `RFP_MIN_DEPTH`, `RFP_MIN_PLY`, `S073` and `-DRFP_MARGIN=75` absent from the file entirely. The check also caught this step's own gap — the guard sweep was uncited until the count came back 2 of 3. Two facts found beyond the step file's premise, both verified over the whole history: the sweeps ran 13:08 to 13:28 against S033's **uncommitted** tree, before `6bd650e` landed at 13:40:34 and introduced RFP at all; and `RFP_MIN_DEPTH` is in no commit (`git log -S` returns only the audit report `e7aa98a`, `git grep` over `git rev-list --all -- src/` returns nothing) while `git log -S '#ifndef RFP' --all` returns no commit. So `-D` never regressed, it was never committed, and `S033_rfp_sweep.tsv` is reproducible at HEAD by no hand edit at all — only `S033_rfp_ply_sweep.tsv` maps onto the shipping engine. Shipping row re-measured at `d7901e3`: `tools/search_bench.py ./build/src/chesso 9` = 292313 + 1026739 + 103001 = **1422053**, best `c3d5 e2a6 d7c8q`, exact. `-Werror` reproduced once, g++ 13.3.0, probe build outside the tree and deleted after. `moltke --validate` clean; `cmake --build build -j12 && ctest --test-dir build -L fast` **12 of 12, 0 failed, 19.65 s**; `./clang-format.sh --check` exit 0; `tools/plan_prose_check.py` 0 flagged. No `src/` change, per `excludes:`. `README.md` owner-written, no change needed; `MANUAL.md` and `DEV_MANUAL.md` checked, no change needed — `DEV_MANUAL.md:17` already delegates the file list to `adocs/data/README.md` and `:193-203` already quotes 1422053 at S033 |
| S068 | the live documents that quoted the old node count and the old default no longer do | `grep -n '1422053\|RfpMargin' DEV_MANUAL.md MANUAL.md README.md adocs/data/README.md` over the four documents a session actually executes against, each hit traced to the code path that produces it. `README.md` is owner-written and read only. Immutable history — `adocs/plan_done/`, `adocs/decisions.md`, `adocs/audit/`, earlier rows of this ledger — keeps 1422053 and is correct as history; procedure, not a test | four live claims corrected. `MANUAL.md:77` `RfpMargin` default 100 -> 75, which the surface guard does **not** cover: `test_uci_surface` requires each name in backticks and the release build's three golden `option name` lines, and the tune build's generated lines supply their own defaults, so nothing was holding that column. `DEV_MANUAL.md:269` 1422053/0.23 s -> 1216123/0.19 s and `:277`'s series gained the S068 entry; those two are the **pre-edit** lines, and the same text sits at `:272` and `:280` after it. `DEV_MANUAL.md:79` (`:80` after the edit) said "292313 is what the release build reports, to the node" and had stopped being true; re-measured over UCI on `build-tune`, **213509 nodes at `RfpMargin` 75, 292313 at 100**, same pv to the move, so the sentence now names 75 on a measurement rather than an assumption. `adocs/data/README.md`'s "HEAD is margin 100 ... row `3 100 6`" now states both rows and which is HEAD. Three further stale claims in that file were found and fixed while in it, all dated inline: `RFP_MARGIN` and `RFP_MAX_DEPTH` cited at `src/search.cpp:38-39` and `RFP_MIN_PLY` at `:47`, all three moved to `src/search_params.hpp:60`, `:61` and `:70` by `7f15ac4`; the quiescence handoff at `:304` and the RFP check at `:338`, now `:311` and `:348`; and "S073 is the step that makes `-D` work", which S073 landed and did not do — `g++ -fsyntax-only -std=c++20 -Isrc -DRFP_MARGIN=75 src/search.cpp` still fails, reproduced today, now against the X macro's declaration rather than a `#define`. One consequence that cannot be fixed in place: `adocs/plan_done/S068_rfp_margin_retune.md:96`, `:99` and `:145` cite `DEV_MANUAL.md:50-59` and `:61`, correct when written at `7f15ac4` and moved by this commit's own edit to the tune-build section. `plan_done/` is immutable (AGENTS.md section 11), so the drift is recorded here rather than edited away; the target is the section titled "The tune build", now `:50-60` and `:62`. Its `:322` cites `DEV_MANUAL.md:269` and `:277` in the past tense, for where the stale figure was found, and is correct as written. `README.md` owner-written, checked, no change needed |
| S074 | S021's gate names the fast suite's three mate cases by their test titles, and the SPRT clause it already carried is unchanged | `accepts:` now quotes "mate in one", "mate in two is found at the right distance" and "pruning does not hide a mate against the material leader", all three `TEST_CASE_FIXTURE`s in `tests/test_search.cpp` carrying the `fast` label `add_doctest_target` sets in `tests/CMakeLists.txt`. Mechanical check, non-vacuous by construction: it first extracts every `TEST_CASE_FIXTURE` title from the source and refuses with exit 2 unless all three are real cases, then requires each quoted verbatim in the `accepts:` field; procedure, not a test | green, **3 of 3 quoted, 3 of 3 real**. Observed red against `git show HEAD:adocs/plan_todo/S021_aspiration_windows.md`: **0 of 3 quoted**, precondition still 3 of 3, so the red is the gate and not the extractor. The three cases run green at HEAD as the baseline S021 inherits: `build/tests/test_search -tc=` the three titles, **3 passed, 0 failed, 123 assertions**. The SPRT clause is byte-identical — the edit only appends after it, and DEC-063's bounds paragraph is untouched. No code, no play change. `moltke --validate` clean; `cmake --build build -j12 && ctest --test-dir build -L fast` **13 of 13, 0 failed, 20.85 s**; `./clang-format.sh --check` exit 0; `tools/plan_prose_check.py` 0 flagged. `README.md` owner-written, no change needed; `MANUAL.md` and `DEV_MANUAL.md` checked, no surface or play change |
| S021 | aspiration windows are not a strength regression, and the verdict is recorded whatever it is | One SPRT, `adocs/data/S021_sprt.sh`, between two ordinary Release builds -- candidate from the working tree at `(5, 50, 400)`, reference `2b54a4f` built into `.ref-builds/2b54a4f/` -- on `books/8moves_v3.pgn` in random order, `tc=10+0.2`, `Hash=16`, `Threads=1`, concurrency 12, `model=normalized`, `alpha=0.05 beta=0.05`, the adjudication of `fastchess.sh:49`. Bounds `elo0=-5 elo1=5` and not the script default, under DEC-063 and on this step file's own paragraph that predicted the failure mode before S068 hit it. The script header carries the pre-registered interpretation of all three outcomes, written before launch; procedure, not a test | **`SPRT ([-5.00, 5.00]) completed - H1 was accepted`** in `Total Time: 00:36:41`, 824 games, `Elo: 35.12 +/- 19.06, nElo: 44.04 +/- 23.72`, `LOS: 99.99 %`, `Ptnml(0-2): [29, 80, 142, 101, 60]`, `LLR: 2.97 (100.8%)`. **+35.12 is not reported as the effect size** -- an SPRT stops early exactly when the observed effect has run favourable, so the estimate is biased upward by optional stopping and one run has nothing to pool it against. Verdict recorded as **not a regression of 5 Elo or more, sign positive at LOS 99.99 %**. The expectation written into the step file before the run was +9 +/- 17 and the outcome sits outside it: DEC-019 from the other side, where a reported figure sized the bounds correctly and predicted the magnitude badly. Contamination checked before the number was read, not after (DEC-020): both `CMakeCache.txt`s `Release` / `-O3 -DNDEBUG` / `/usr/bin/c++` / `CHESSO_TUNE:BOOL=OFF`, the reference worktree clean at `2b54a4f` with `grep -c ASPIRATION src/search_params.hpp` = 0, and the candidate snapshot md5-identical to `build/src/chesso` |
| S021 | the fast suite's three mate cases are green at the window schedule that ships, and a case exists that the schedule can actually fail | The three named cases run as their own invocation at the shipping schedule: `./build/tests/test_search -tc="mate in one,mate in two is found at the right distance,pruning does not hide a mate against the material leader"`. **They cannot discharge S074's clause on their own** -- all three call `search()` directly at one fixed depth, where no window is ever narrowed, so the schedule they are asserted "at" never runs in them. `engine: aspiration windows` in `tests/test_engine.cpp` is what runs it: two positions driven through `iterative_deepening_search` to depth 10, both scoring in centipawns for eight iterations and reporting a mate at depth 9, so the mate arrives through a band 50 wide and the iteration must fail high and repeat. Both were found by measurement -- the `2b54a4f` binary, which has no windows, run over every FEN in `tests/assets/test_jsons/` and asked where a mate first appears -- and not chosen. Three preconditions, all asserted: the mate first appears above `AspirationMinDepth`, the iteration below it scores in centipawns, and `uci_last_aspiration_failures() > 0` | green. Three named cases **3 passed, 0 failed, 123 assertions**; `engine: aspiration windows` **2 passed, 0 failed, 69 assertions**. **Observed red twice, both with every precondition still holding.** Widening removed entirely, and then the failure counted but never re-searched: identical output both times, `REQUIRE( iterations[test.first_mate_depth - 1].kind == "mate" )` / `values: REQUIRE( cp == mate )` on `r3r1k1/pp3pbp/1qp1b1p1/1BB5/3P4/Q1n2N2/P4PPP/3R1K1R b - - 5 18`, 32 of 33 assertions passing before it. So the mate is genuinely lost without the re-search, and the second stub is the sharper red: it keeps `uci_last_aspiration_failures()` non-zero, so the case fails on the answer rather than on its own precondition |
| S021 | the live documents that quoted the old node count and the old absent list no longer do | `grep -n '1216123\|aspiration' DEV_MANUAL.md MANUAL.md README.md adocs/specs.md adocs/data/README.md`, each hit traced to the code path that produces it. `README.md` is owner-written and read only. Immutable history keeps 1216123 and is correct as history; procedure, not a test | `MANUAL.md:176` moved aspiration windows out of the known-absent list and `:85-87` gained the three tune options, which `test_uci_surface` requires by name in the tune build and which is what made that build red until the rows were written. `adocs/specs.md:80` and `:83` moved the feature between the two rows with the schedule, the sweep figure and the verdict. `DEV_MANUAL.md:272` 1216123 -> **1225840**, re-measured on an idle machine over three interleaved runs, and `:280` gained the S021 entry plus the paragraph the number earns: the count went **up** 0.8 % on those three positions while the 300-position sweep says 7.5 % down and the SPRT accepted H1, so the section now says what the bench is for and what it is not. Citations shifted by this commit's own two added lines were corrected in it: `adocs/data/README.md` `src/search.cpp:348-350` -> `:350-352`, `:311` -> `:313`, `src/search_params.hpp:41-91` -> `:41-126` twice, and a dated note on the `:99:24` inside a verbatim g++ transcript, which is left as the compiler printed it. Two in `DEV_MANUAL.md:614` were already stale by twenty lines before this step and were fixed while in the paragraph: `src/search.cpp:125`, `:152`, `:136-137` -> `:147`, `:179`, `:160-161`. **Not fixed, and named rather than silently worsened**: `adocs/plan_todo/S079_s063_widened_citation_scope.md`'s table of correct locations was already wrong at `2b54a4f` -- it gives `:560` and `:564` for sites that were at `:570` and `:574` there -- and this commit adds two more. S063 and S079 own that pass and it is theirs to recompute, not to adjust |
| S075 | `--lambda 0` fits exactly what the tuner fitted before S075, bit for bit on the same seed and data | Three binaries over one fixture, not a test: the pre-change binary built from `0043c31` and stashed before the edit, then the post-change binary twice, once with no `--lambda` and once with `--lambda 0`. Fixture is the first 1000000 rows of `.tuning/selfplay_v2.tsv`, `--freeze tempo,piece_placement --seed 1 --validation 0.1 --threads 12`, run twice: `--epochs 500 --report 10` at the fitted K, and `--epochs 300 --report 10 --patience 100 --k 1.5` so that the fit moves at every report and the emitted constants are a fitted vector rather than the starting one — on the whole corpus at the fitted K the shipping weights are already near the optimum, `best` stays at the start, and an equality over the starting constants would assert nothing. Non-vacuity is the fourth run: `--lambda 0.7` over the same fixture must emit something else; procedure, not a test | **identical.** At the fitted K, 56-line logs identical bar the `--out` path each was told to write; at `--k 1.5`, 34-line logs identical the same way, 30 of 30 reports starred, held-out `0.129998 -> 0.120249`. Emitted constants identical across all three, `sha256 9bbf81126aa44f93c90dcc2bb08888183306c9e2717562e980bb5646a7dbdbaa` from the first `#define`, and the whole emitted file differs from the pre-change one by exactly one added line, `// lambda     0.0000  target = lambda * sigma(K * score) + (1 - lambda) * result`. `--lambda 0.7` on the same fixture emits `d0d4eb8a7174995159a8687e7ef969348e92c328c09df130c2d9ddb8d1e0a040` and reports `wdl 0.123778` against `validation 0.033419`, a factor of 3.6 between the two metrics that is the whole reason DEC-064 forbids ranking lambdas on the training error. Re-verified after `clang-format.sh` reflowed the file: same sha. The 11003693-row sweep independently reproduces S065's split to the row — `119998 games, 9903305 train, 1100388 validation (10.0002%)` | 
| S075 | exactly one candidate goes to an SPRT, and the verdict is recorded whatever it is, including zero | `adocs/data/S075_sprt.sh`, written and committed **before** the sweep ran, carrying the pre-registered reading of all three outcomes, the bounds argument (`elo0=-5 elo1=5` under DEC-063) and the attribution limit. `adocs/data/S075_lambda_sweep.sh` rule 3, also pre-registered: if lambda 0 wins the selection there is no candidate and no match is played; procedure, not a test | **zero candidates, no SPRT, verdict recorded as zero.** No lambda produced a vector predicting held-out outcomes better than the constants that ship, so nothing was eligible to be played and the three to four and a half hours a verdict costs were not spent on a vector already measured worse on the metric that would have selected it. The script is committed unrun for whoever re-asks this on a regenerated corpus. The attribution limit it was written to carry **dissolved before it was needed**: the lambda 0 control moved held-out error by 3e-06 over 5000 epochs and a dozen weights by +/-1, so "the candidate is a refit as well as a changed label" stopped being a confound — measured, not argued away. S005, S006 and S015 are the precedent for keeping a feature whose measured value is zero; `--lambda` ships at 0, which is bit for bit the fit that came before it | 
| S076 | the deduplicated corpus is refitted and the fitted vector is not the incumbent | `adocs/data/S076_dedupe_fit.sh` at S065's and S075's settings -- `--freeze tempo,piece_placement --seed 1 --validation 0.1 --threads 12 --lambda 0`, K fitted -- so the corpus is the only thing that differs from the fit that produced the shipping weights. `.tuning/diff_fit.py` against `HEAD` is the gate the script pre-registers: if nothing moved, no match; procedure, not a test | 1378 s, best at epoch 1600, stopped by patience at 3600 of 5000. Held-out error against the game result over its own rows **0.119608 -> 0.119458**, 1.5e-04 against S075's 4e-05 noise floor. **`fitted K = 0.7801` against the 0.7595 S075's control fitted from the same starting constants on the full corpus** -- K is fitted at the starting parameters, so with those held the corpus is the only input that moved, and 1.9 % of rows moved the score-to-outcome scale by 2.7 %. The weights moved an order of magnitude further than the same budget moves them on the full corpus: psqt rook mg mean **-51.2** against +2.3, rook eg **+59.9** against -1.8, largest single square **761** against 74, while the material defines moved by -1 and +2. That contrast is the attribution the step does not spend a second verdict on |
| S076 | the ten pinned absolute anchors are re-derived and not copied, and the truncation guard is re-measured rather than relaxed | `.tuning/anchors.py`, a second implementation of `evaluate()` for the pinned positions written from the specification, run against the pasted weights; and `build/tools/truncation_scan`, new and tracked at this step, over all 10795695 rows. The suite is the check on both: `test_evaluation`'s "each piece is worth what the tables say" and four `REQUIRE_EQ`s in `test_search` assert the derived values as exact equalities, and `test_eval_model`'s "the pinned positions reach the truncation bound" asserts the re-measured four still disagree with the float model by more than 2.0, one of them past 2.8 | **10 of 10 anchors reproduced** -- 135, 244, 325, 563 with `evaluate_cheap` 567, 787, 0, 198, -569 and the -505 quiescence composite -- and the suite green on all ten as exact equalities, so the pins are re-targeted and not relaxed. Five of the six piece anchors rose while `PAWN` fell by 1: the tables moved, not what a piece is worth. **Observed red before the re-target**, which is how the paste announced itself: `REQUIRE( evaluate(&game.board) == test.score )` / `values: REQUIRE( 135 == 125 )`, plus 563/509, 198/224, -505/-446 and -569/-537 in `test_search`. The truncation guard went red the same way -- S065's four positions fell to 0.083333, 0.958333, 1.833333 and 1.000000, and `CHECK( worst > 2.8 )` read 1.833333 -- because **a residual belongs to the weights and not to the position** (DEC-057). Re-measured: 135399 of 10795695 rows past 2.0, 99 past 2.8, **30 at the arithmetic maximum 69/24 = 2.875**; the four pinned are drawn from those 30, spanning phase 5 to 23 with both sides to move, and phase 17 has no maximal position under these weights so the middle two are 11 and 19. Thresholds untouched. The scanner is checked against the test rather than trusted: on S065's four it prints the same six digits the red run printed |
| S076 | exactly one candidate goes to an SPRT against the weights that ship, and the verdict is recorded whatever it is | One SPRT, `adocs/data/S076_sprt.sh`, written and committed **before** the fit ran and carrying the pre-registered reading of all three outcomes, the bounds argument (`elo0=-5 elo1=5` under DEC-063) and the attribution limit. Candidate is the working tree carrying the one emitted vector; reference is `a579f46` built into `.ref-builds/a579f46/`. `books/8moves_v3.pgn` in random order, `tc=10+0.2`, `Hash=16`, `Threads=1`, concurrency 12, `model=normalized`, `alpha=0.05 beta=0.05`, the adjudication of `fastchess.sh:49`; procedure, not a test | **`SPRT ([-5.00, 5.00]) completed - H1 was accepted`** in `Total Time: 00:46:48`, 1044 games, `Elo: 26.68 +/- 16.40, nElo: 34.44 +/- 21.08`, `LOS: 99.93 %`, `Ptnml(0-2): [38, 102, 186, 134, 62]`, `LLR: 2.95 (100.1%)`. About 1338 games/h, the same throughput S021 and S068 measured here. **+26.68 is not reported as the effect size**: an SPRT stops early exactly when the observed effect has run favourable, and one run has nothing to pool against -- S068's pooled estimate fell from +12.18 to +5.02 on that correction. Verdict recorded as **not a regression of 5 Elo or more, sign positive at LOS 99.93 %**, and the paste is kept. Contamination checked before the number was read (DEC-020): both `CMakeCache.txt`s `Release` / `-O3 -DNDEBUG` / `/usr/bin/c++` / `CHESSO_TUNE:BOOL=OFF`, the reference worktree clean at `a579f46` with `#define PAWN 95` against the candidate's 94, and the candidate snapshot md5-identical to `build/src/chesso` at the completing commit |
| S077 | an emitted header names the engine commit and the corpus by content, and two runs on the same corpus and seed emit the same header | `build/tools/tuner` twice over the same 200000-row fixture at `--freeze tempo,piece_placement --seed 1 --validation 0.1 --epochs 20 --report 10 --threads 12`, and `sha256sum` over the same file as the independent check. Also `.tuning/apply_fit.py --dry-run` and `.tuning/diff_fit.py` against a stamped header, since both parse what the tuner writes; procedure, not a test | **the two emitted files are byte identical**, and the two logs differ only in the `--out` path each was told to write. The header carries `// engine     d9fb0b2-dirty, the commit this tuner was built from` and `// corpus     sha256 ffdcd801ab59c58390503bfee468a5caeb71a6bb38c012904ec87675382533de` with `200000 rows, 12995744 bytes`, against `sha256sum`'s `ffdcd801ab59c58390503bfee468a5caeb71a6bb38c012904ec87675382533de` and `wc -l` / `wc -c`'s 200000 and 12995744 -- **the same digest from a tool that shares no code with this one**. Repeated on the real 706 MB corpus: `0a6b59b6af9f50c4360cac7c87c33250318e712250f2653eb09bcb9f0b64b69f` from both, which is also the value S076's dedupe log recorded, in **2.70 s** against `sha256sum`'s 1.29 s. The paste tooling is unaffected: `apply_fit.py --dry-run` reads `5 defines, 2 tables, 10 arrays, 2 scalars` from the stamped header and `diff_fit.py` reports its material defines |
| S078 | S060 stops arguing from an absence and names the template S026's two cases copy, including how it asserts its preconditions | `adocs/data/S078_body_check.py`, run from the repository root over both step files. Non-vacuous by construction: it first extracts every `TEST_CASE_FIXTURE` title from `tests/test_search.cpp` and `tests/test_evaluation.cpp` and the four source lines the bodies must quote, and exits **2** unless all of them are really there; only then does it test the bodies. Whitespace-collapsed rather than line-wise on purpose — the step files are hard-wrapped and a quoted title routinely splits across two lines, which is the mistake S062's first checker made. Procedure, not a test | green, **precondition 4 source lines and 4 titles present, 0 flagged, exit 0**. **Observed red at HEAD: 7 flags, exit 1**, precondition still passing, so the red is the gate and not the extractor — S060's `no test name asserts it` sentence, the missing template title, all three preconditions and the depth loop, plus S061's range citation. **The precondition arm was observed firing separately**: pointed at `git show 6bd650e^:tests/` — the tree before S033's implementation commit added the case — it exits **2** naming the title and two of the three `REQUIRE` lines, which is the rename hazard it exists to catch. Neither `accepts:` changed and the check asserts that too |
| S078 | S061's citation names the band case by its title rather than by a line range | Same check, and the range was independently wrong: `tests/test_evaluation.cpp:643-704` against a case that runs `:662-723` at HEAD — off by nineteen lines, not the nine S078's own body predicted, because `tests/test_evaluation.cpp` moved again between the audit and the execution. The title `bands are strictly ordered` is unchanged across both. Procedure, not a test | green, **0 range citations of that file left in S061**. Every line number in S078's own evidence had drifted too and is tabulated in the step file under "As executed" rather than edited away: nine lines in `test_search.cpp`, nineteen in `test_evaluation.cpp`. No code, no play change. `moltke --validate` clean; `cmake --build build -j12 && ctest --test-dir build -L fast` **16 of 16, 0 failed, 19.61 s**; `./clang-format.sh --check` exit 0; `tools/plan_prose_check.py` **67 ids in prose, 0 sentences flagged**. `README.md` owner-written, no change needed; `MANUAL.md` and `DEV_MANUAL.md` checked, no surface or play change |
| S088 | the rated run's interval meets ±30 and the anchor sweep is re-solved over every reference | `adocs/data/S088_solve.sh` over `S088_rated_c6.pgn`, `ordo 1.2.6 -s 1000 -F 95 -n 12 -W -D` once per anchor, anchors read from the CCRL Blitz list at run time by `tools/ccrl_rating.py`. **Validated against known data before use**: run first on `S088_rated_c12_INVALID.pgn`, where it reproduced all five hand-computed anchors to the decimal and the exact intervals | **interval met** — ±23.7, ±24.0, ±24.4, ±24.8, ±27.7, all inside ±30 on one run of 3340 games rather than two combined as S087 needed. chesso solves to **2533.8 / 2476.7 / 2586.0 / 2597.6 / 2598.5** anchored on Blunder 7.1.0 / Leorik 2.1 / Blunder 8.5.5 / Stash v21.0 / Leorik 2.4. Overall 40.2 %, +993 =702 -1645. A bug in the script was found and fixed by running it: it never created its output directory, so it failed on a clean path. Full record `adocs/data/rating_2026-08-18_S088_ccrl_blitz.md` |
| S088 | the anchor spread is reported in full, the dissenter is named, both figures are given, and the quoted one is a recorded decision rather than the smaller number | Five solves of the same 3340-game PGN, one per anchor, with and without the dissenting reference. DEC-077 | **the step's own premise FAILED and that failure is the reported result.** A third family did not close the spread: **121.8 Elo** over five anchors against S087's 83.1 over four. Leorik 2.1 is named as the low outlier and dropping it leaves **64.7**, still twice the ~30 allowed, so there is no single dissenter. **The quoted figure is the five-anchor mean 2559 — the lower number carrying the larger spread** — with 2579 reported beside it and not quoted. What the third family bought is the *shape*: ex-Leorik 2.1 the solved rating rises monotonically with the anchor's own rating and flattens, and across a CCRL span of 440 Elo the measured differences span **375.3, a ratio of 0.853**. That is compression, not scatter, and S087's argument that a scale artifact was excluded no longer holds |
| S088 | a rating figure is attributable to an engine, so a changed number means a changed engine | `git diff --stat a9f2b33 HEAD` over `src/`, `tests/`, `CMakeLists.txt` and `cmake/`, plus re-solving S087's own combined PGN with the identical `ordo` command as a control. Procedure, not a test | **empty diff over all four paths**, so S088 re-rated the same binary S087 rated. The control **reproduced S087's four anchors exactly** — 2559.3 / 2492.5 / 2574.6 / 2575.6 with ±25.5 / ±24.1 / ±24.4 / ±28.3 — which is what ruled the solver out before the games were blamed. S087's 2570 sits between S088's 2559 and 2579: **the instrument changed and the engine did not**, which is the correct outcome for a step whose only product was a reference engine |
| S088 | concurrency is measured rather than argued, and the run that was voided is kept as the evidence for it | Three runs at 668 games a pairing: S087 (4 engines, concurrency 12), S088 voided (5 engines, 12), S088 valid (5 engines, 6). Procedure, not a test | **concurrency does not explain Stash's forfeits**: 3 of 668 at 12 (0.45 %) against 1 of 668 at 6 (0.15 %), and the concurrency-6 forfeit was a 25360 ms hang rather than a margin overrun. DEC-073 dropped the concurrency to fix this and it did not move; DEC-075 put it back. **Concurrency 6 costs 2.1x**, measured live: 6.2 of 12 threads busy, 10.6 games/min against 22.6, 5 h 02 m 49 s against 2 h 27 m 28 s for the same 3340 games. **One anomaly is left open with numbers rather than explained away**: chesso scored 37.6 % / **44.3 %** / 39.1 % against Blunder 8.5.5 across the three runs, so the five-engine concurrency-12 run is the outlier, and since DEC-075 puts every later run at 12 the next one is the test |
| S089 | the hard limit never exceeds the time the clock actually has, on a short clock | `test_engine`, `engine: uci layer`, `the hard limit never exceeds the clock`, label `fast`, over `compute_search_time_budget()`. Nineteen clocks including the five S036 rows and six sudden-death rows the fabricated `movestogo 20` used to reach. Asserts `soft <= hard`, `hard <= remaining`, `hard < remaining` above 1 ms, `hard <= remaining - MOVE_OVERHEAD_MS` wherever the clock holds both the overhead and the floor, and the S036 floor below that | green, **1 case / 158 assertions**. **Observed red** with the clamp to the clock removed: `REQUIRE( budget.hard_ms <= test.remaining ) is NOT correct! values: REQUIRE( 3000 <= 1000 ), logged: remaining 1000 inc 0 movestogo 1` — the hard limit is three times the allocation, so without the clamp it is three times the clock at `movestogo 1` |
| S089 | the budget scales by best-move stability and by a score that has fallen, each factor a constant with a stated range (S073) | `test_engine`, `engine: uci layer`, `the time scale moves with stability and with a falling score`, label `fast`, over `search_time_scale_percent()`. Both directions, both caps, and the neutral point as the precondition every comparison is measured from | green. **Observed red** with the function returning its neutral 100 before either factor is applied: **5 of 12 assertions** — `CHECK( search_time_scale_percent(1, 0) < 100 ) ... values: CHECK( 100 < 100 )`, `CHECK( search_time_scale_percent(0, TM_FALLING_MAX_CP) > 100 ) ... CHECK( 100 > 100 )`, and `CHECK_EQ( search_time_scale_percent(0, TM_FALLING_MAX_CP), 100 + TM_FALLING_PERCENT ) ... CHECK_EQ( 100, 150 )` |
| S089 | a 1 ms clock still returns a legal move, which is the S036 defect | `test_engine`: `a one millisecond clock answers without a stop` (`engine: uci layer`) unchanged, plus `go wtime 1 btime 1` added to `every kind of go answers with one legal bestmove` (`engine: uci go`), which is the suite that owns `require_playable()` and so checks legality rather than only a non-null bestmove line. Since S089 that command takes the sudden-death path instead of the fabricated `movestogo 20` | green, both cases. The S036 watchdog was not touched; the legality half is the addition |
| S089 | the change that alters play is decided by an SPRT against a named commit, and the verdict is recorded whatever it is (INV-6) | `REF=1069cc6 ./fastchess.sh --fast`, candidate `a10ca84` against a `.ref-builds/1069cc6` worktree, `elo0=0 elo1=10 alpha=0.10 beta=0.10`, `model=normalized`, `tc=10+0.2`, `Hash=16`, `Threads=1`, concurrency 12. `REF` was passed explicitly because `fastchess.sh:22` defaults it to the fixed old commit `7b4d9a4`, which would have measured this change against months of unrelated work (DEC-020). Procedure, not a test | **`SPRT ([0.00, 10.00]) completed - H1 was accepted`** in `Total Time: 00:21:59`, 500 games, `Elo: 45.42 +/- 23.47, nElo: 59.71 +/- 30.45`, `Ptnml(0-2): [13, 44, 94, 63, 36]`, `LLR: 2.21 (100.7%)`. **+45.42 is not reported as the effect size** — an SPRT stops early precisely when the observed effect has run favourable, and S068's pooled estimate fell from +12.18 to +5.02 on that correction. Recorded verdict: **not a regression, sign positive**. Contamination checked before the number was read (DEC-020): both `CMakeCache.txt`s `Release` / `/usr/bin/c++` / `CHESSO_TUNE:BOOL=OFF`, the reference worktree clean at `1069cc6`, and the two binaries distinct by md5 |
| S094 | each change that alters play gets its own SPRT verdict, recorded whatever it is (INV-6) | Two matches, dedicated PGN and log paths per run so neither the appended-PGN nor the empty-log trap of S089 could recur. `f1e6d24` vs `bd45afe`, then `22a74f2` vs `7d2da9d`, both `elo0=0 elo1=10 alpha=0.10 beta=0.10 model=normalized`, `tc=10+0.2`, `Hash=16`, `Threads=1`, concurrency 12, built from `.ref-builds/` worktrees | **Both zero, and recorded as zero.** The probe: **no bound reached** in 3000 games and 2 h 07 m, `LLR -1.32`, `Elo -0.23 +/- 9.32`, `nElo -0.31 +/- 12.43`, 49.97 %, `Ptnml [120, 354, 564, 332, 130]`. Standing pat on the stored value: **H0 accepted** in 1954 games and 1 h 23 m, `LLR -2.21`, `Elo -6.40 +/- 11.42`, `nElo -8.64 +/- 15.40`, 49.08 %, `Ptnml [91, 212, 386, 218, 70]` — H0 is "not a +10 improvement", which a true zero satisfies. **0 time forfeits in either**, counted from each run's own PGN (3000 and 1957 games). The zero was predictable and predicted: the probe finds an entry on **0.79 %** of quiescence nodes and the stored value differs from a fresh lazy call on **0.05 %** of them |
| S103 | the stored value never disagrees with a fresh `evaluate()`, over more than one search | Temporary counters at the reverse-futility block comparing `tt_eval` against a fresh call on every hit, removed before the commit. Procedure, not a test | **3368027 of 14589403 calls, 23.09 %, and 0 disagreements** over 300 positions from `adocs/data/S018_raw.tsv` at depth 10 through one engine process, table left warm across positions as it is in a game; **173440 of 1617617, 10.72 %, 0 disagreements** over the three `search_bench` positions at depth 12. DEC-079's 9.8 % was one position on its own — the rate rises with how much has been written to the table |
| S104 | three distributable targets exist, they are genuinely different code, and the one that ships to a rating list is named | `./build_release.sh all`, then `objdump -d` per binary counting BMI2 (`shlx|sarx|shrx|bzhi|mulx|pdep|pext`) and AVX2 (`vpxor|vmovdqu|vpaddw`) instructions. `cmake/arch.cmake` puts every flag set to `check_cxx_compiler_flag` before using it, so a silently dropped flag is a configure-time `FATAL_ERROR` rather than a target named for an instruction set it does not target | green, and distinct: **`bmi2` 409 BMI2 / 75 AVX2, `avx2` 0 / 75, `portable` 0 / 0**, all three 125 popcnt. **`bmi2` is what ships.** `__BMI2__` is the switch `avx2` turns off and is what S032's magic path will key on. **`-march=native` never ships**: it is the default for `build/`, which is the directory that gets measured, and `build_release.sh native` exits 2 by name |
| S105 | the faster control does not cost games on the clock | **1000 games at 8+0.08**, `adocs/data/S105_calibration_after.pgn`, counted from the PGN and not from the log. Non-vacuous by the same pass classifying all 1000 into 771 `adjudication` and 229 `normal`, so it demonstrably sees terminations. Checked **before** the throughput and draw figures were read, because 8+0.08 leaves `MOVE_OVERHEAD_MS 50` about 2.5x less room per move than 10+0.2 | **0 time forfeits in 1000**, and 0 in the 1000 of the old regime beside it. The log-based check would have been vacuous again: **both** `fastchess.log` files were **0 bytes**, the WARN-only default S089 diagnosed |
| S105 | a run's census is its own run's, and the run ends in a marker a watcher can exit on | `fastchess.sh` writes to a stamped `OUT` directory and counts terminations, draws and forfeits out of it, then prints `SPRT-RUN-DONE`; `SPRT-RUN-FAILED` on every abort path, with the EXIT trap's `if` written so `set -e` cannot turn a clean run into a failed one. Procedure, not a test | the S089 trap is closed by construction: the fixed `/tmp/fastchess_<tag>.pgn` held 587 games after a 500-game run and read 421/166 instead of 359/141. The calibration's own watcher exited on its marker — `"outcome": "success marker", "exit_code": 0` in `.git/moltke_watch/` |
| S105 | `rating.sh` staying above the SPRT's hash is deliberate | `rating.sh`, raised 64 to 128 with the reason in the comment beside it. The two scripts match different invariants: the gauntlet reproduces the list's absolute regime (128 to 256, DEC-089), the SPRT reproduces table pressure at its own much shorter control (DEC-088) | `hash_mb=128`. 64 matched neither, which is what the change fixes; `rating.sh`'s time control stays 10+0.2 and is S128's question, excluded here |
| S106 | each arm of the `normalize_score`/`de_normalize_score` pair fails a test when its ply term is dropped, in **both** searches | Four store-side and probe-side cases in the same suite, driven at ply 3 because no call to `search()` places a node there: `quiescence()` on `7k/6Q1/6K1/8/8/8/8/8 b - - 0 1` for the negative arm, `negamax()` on `7k/Q5b1/6K1/8/8/8/8/8 w - - 0 1` (Qxg7 mate, stockfish depth 20) and on `4k3/Q7/4K3/8/8/8/8/8 b - - 1 1` (MATE_IN_2_W_POS after e5e6, python-chess) for both arms. The read side is S094's existing pair at `:823`/`:833` | all four **red** under their own mutation: `:1281 REQUIRE_EQ( entry->score, -MATE_MAX_LOCAL )` — `values: REQUIRE_EQ( -48997, -49000 )`; `:1324 REQUIRE_EQ( entry->score, MATE_IN_ONE_PLY )` — `values: REQUIRE_EQ( 48996, 48999 )`; `:833` — `REQUIRE_EQ( 49000, 48997 )`; `:823` — `REQUIRE_EQ( -48999, -48996 )`. **The first version of three of these passed under their own mutation**: a ply-0 precondition writes an exact entry, every probe accepts one, and the ply-3 drive answered out of the table without reaching a store. `tt_reset()` per drive |
| S107 | a quiet move that gives check is admitted to the killer, history and countermove tables | `tests/test_search.cpp`, suite `search: move ordering state`, case "a quiet move that gives check enters the ordering tables". Driven at a node, not through `search()`: a countermove needs a non-zero `prev_move` and the root passes 0, so a root-only test can reach two of the three tables and never the third. `negamax(48499, 48500, 1, 1, ..., f8g8, false)` on `6rk/6pp/7N/8/8/8/8/6K1 w - - 0 1`, where only a mate score clears beta — so the fail-high move is *the mate* and no later ordering step can move the case. Position from a tool (DEC-023): python-chess gives the whole mating set as `[('Nf7#', 'h6f7', False)]`, stockfish on the position after `h6f7` gives `info depth 0 score mate 0` / `bestmove (none)`. The case asserts what the move is — not a capture, not a promotion, gives check when made — then **presence only** in all three tables, never a magnitude or a formula, so S093's bonus, indexing and ageing rewrite does not touch it | green; **red at HEAD**, `:556 REQUIRE( killer != 0 )` — `values: REQUIRE( 0 != 0 )`, with the fail-high precondition `REQUIRE(score >= BETA)` passing first, so the red is the table and not a node that never cut off |
| S100 | the feature extraction is verified by an independent count test per term, and the columns a fit regresses on are the columns their names claim | `tests/test_tuner_gradient.cpp`, case "every stored column holds the feature its index names": every one of the 27 stored feature columns per row, against the same feature recomputed from the FEN text outside `load()`, plus the piece list, its offsets and the phase. It is deliberately **not** a check of what a feature means — it calls the extractors `load()` calls, so a wrong definition would agree with itself; `test_eval_model.cpp`'s per-colour hand-computed cases and its engine differential are what hold those, and they were audited rather than reinvented (`:1015-1130`, `:933-958`, `:966-1000`). What this catches is the span between a correct extractor and a fitted weight: a misaligned base, a wrong stride, a column pair stored in the other order. Reachable at all only because S100 moved `dataset_t`, `load()`, `evaluate_position()`, `error_range()` and `gradient()` out of `tools/tuner.cpp`'s anonymous namespace into `tools/tuner_model.hpp` — the S041 precedent | green, and green on the corpus too: `build/tools/feature_audit` re-extracts every column of all **10795695 rows of `.tuning/selfplay_v2_dedup.tsv` for 0 disagreements**. The fixture's non-vacuity is its own case — every term column non-zero in both directions, and both sides to move present |
| S137 | the live documents that said the refusal was unobservable no longer do | `grep -rn 'cannot tell you it refused\|log.hpp:35\|164123' DEV_MANUAL.md MANUAL.md adocs/specs.md tools/ tests/`, each hit traced to the code path it describes. `README.md` is owner-written and read only | five corrected: `MANUAL.md`'s tune-build paragraph and `DEV_MANUAL.md`'s two (the tune build section and the `check` mode one), `tools/spsa_driver.py`'s module docstring and `probe_nodes` docstring, and `tests/test_spsa_driver.py`'s clamp docstring -- the last three outside this step's `touches:`, corrected because this commit is what falsified them. Two stale measurements found while in those paragraphs and re-measured at this commit: `DEV_MANUAL.md`'s `RfpMargin` series **164123 / 223454 / 476911 / 743308 -> 164302 / 223857 / 474204 / 742729**, best move `c3d5` throughout, last taken at S103's commit and moved since by S104, S106 and S107; and `adocs/specs.md`'s "the ten parameters in `src/search_params.hpp`", which have been 22 since S089 -- the count is now not written there at all, because `test_search_params` is where it is pinned. Line numbers were dropped from the `src/log.hpp` and `src/search_params.hpp` citations in the same pass, in favour of the symbol. `README.md` checked, owner-written, no change needed |
| S138 | every `path:line` citation in a pending step file resolves to what the citing sentence says it holds | `tools/plan_prose_check.py --citations`, new. Three exact string comparisons over the 201 gated code citations in the 56 pending step files: `BOUNDS` (path absent at HEAD, or line past end of file), `ANCHOR` (a doctest title quoted on the citing line or the one above it is not the test the cited lines open), `DRIFT` (the cited lines hold different text now than in the commit that last wrote the step file). Document citations and bare `:line` continuations are counted and printed but do not gate — the first because `adocs/` is rewritten at every completion by design, the second because same-paragraph path inheritance produced sixty impossible line numbers when it was tried, S095's `(transposition_table.cpp:96-103), and :449` being the case that killed it. Not in ctest, deliberately: any source commit shifts lines under fifty step files at once, so gating the suite would make red the normal state and this check the thing weakened to clear it | green, **0 flagged, exit 0**. **Observed red against a throwaway worktree at `04bcc43` with the same checker: 107 flagged over 56 files, exit 1** — 101 `DRIFT`, 5 `ANCHOR`, 1 `BOUNDS`; the audit's own census was 70 of 147 because its regex took full `path:line` citations only. Non-vacuity on the corrected `S109:220` citation, reverted after each: `:1266` (a real test, the wrong one) → `ANCHOR`; `:1888`, off by one → `ANCHOR`; `:99999` → `BOUNDS`. Every one of the 89 mechanical relocations verified independently — content at the new lines in the worktree byte-identical to content at the old lines at each step file's baseline commit, 89 of 89 — and every hand-resolved target asserted to contain what its sentence claims, 24 of 24 |
| S138 | the steps that add pruning point at the mate tests that guard it | `grep -rn ':1264\|:1270\|:1274\|:1291\|:1313\|:1314' adocs/plan_todo/`, then each hit read against `grep -n TEST_CASE tests/test_search.cpp`. The `ANCHOR` class checks it mechanically from then on, since all eight now carry the title as well as the line | **eight steps, not the audit's five**: S091, S095, S097, S098, S109, S113, S114, S116. S095, S113 and S116 were missed by `grep test_search.cpp:1274` because they write the gate as a bare `(:1274)` continuation, and all three add pruning or a reduction — internal iterative reduction, ProbCut, razoring at depth one. 13 sites re-pointed to `tests/test_search.cpp:1887` and `:1926`, each naming `"pruning does not hide a forced mate"` or `"pruning does not hide a mate against the material leader"` verbatim, so the next drift degrades to `ANCHOR` rather than to silence |
| S145 | a forced mate whose distance is proved is never mis-scored, and every mate in two is found at the first iteration that can hold it | `test_engine` "a proved mate is never mis-scored, and every mate in two is found on time", 48 constructed positions from `adocs/data/S145_mate_set.tsv` driven through the engine's own `iterative_deepening_search` to `2m - 1 + 8` — not `search()` at one depth, which is what the old gate did and what cannot tell a lost mate from a postponed one. Three preconditions per case before any search: the root is reachable, every guarded defender node on the mating line is not in check, and at least one of them has a static score above the margin reverse futility would subtract at that node's own ply. Distance proved by exhaustive AND/OR enumeration over `python-chess`, iterative-deepening in the distance so every shorter distance is refuted; corroborated by `stockfish` at 4000000 nodes in its own process | green, **2182 assertions in 0.66 s**, fast suite 18 of 18 in 14.89 s. **Observed red under a stated mutation**: `RFP_MIN_PLY` 3 → 1 fails on `first_exact == minimum` at "iteration 5 and not 3" for a mate in two — the failure the three-position gate could not produce, because at RfpMaxDepth 6 it would have found that mate and passed. Asserted: 0 mate scores with the wrong sign and 0 closer than the proved minimum, over 12 reverse-futility settings times 48 positions; 16 of 16 mates in two at delay 0; mate in three at or above a floor of 7. Recorded and not asserted, because a floor of zero asserts nothing: **mate in four 0 of 8 and mate in five 0 of 8** |
| S145 | the floor and the ceiling are measured against the set separately, each with the other held | `adocs/data/S145_rfp_sweep.py`, per setting **and per mate distance**, output kept at `adocs/data/S145_rfp_sweep.log`. One fresh engine process per setting, because python-chess `configure` leaves unnamed options as they were and a reconfiguring loop measures the union of everything it has been through — that mistake produced a table where turning null move pruning off appeared to fix a missed mate and it was reverse futility from the row before | **`RfpMinPly`, ceiling held at 15**: 0 and 1 give 19/48 with a mate in two delayed up to 7 iterations, 2 and 3 give 24/48 with every mate in two immediate, 4 and 5 give 27/48. **`RfpMaxDepth`, floor held at 3**: 34/48 at 0, 27/48 at S033's 6, 24/48 at the shipping 15 and unchanged at 63. The deep classes move only with the ceiling — 4 of 8 and 3 of 8 at 0 against 0 and 0 at 10 and above. So DEC-095's floor of 2 is the value at which the mate in two class is whole, 2 and 3 are indistinguishable over all 48, and the ceiling S085 tuned from 6 to 15 is a trade with a measured cost, which is S148 |
| S145 | breadth over mates the engine actually reached, scored as a count with a floor and never per position | `adocs/data/S145_mined_set.py`, 318 positions — the final position of one game each from 6000 games of `.spsa/S085/games.pgn`, deduplicated by FEN, labelled by `stockfish` at 200000 nodes. `score --floor 143` exits non-zero below the floor or on any wrong sign. Not a `ctest` case: a count needs hundreds of positions and the fast suite has 15 s | shipping build at depth 10: **147 found with the right sign, 146 exact, 0 wrong sign**; the tune build at `RfpMinPly` 1 and 0 reads 140 and 139. Floor 143, placed between the shipping value and the removed-guard value. What it can and cannot resolve is stated rather than assumed: it separates a floor of 1 from a floor of 2 and it does **not** separate 2 from 3 |
| S145 | every SPRT from now on checks the mate scores both engines report | `fastchess.sh` passes `-check-mate-pvs`, verified against the installed `fastchess alpha 1.8.1 20260720-daa3ea2` and against a real four-game match rather than against `--help` alone. `test_fastchess_script` still green, so the script still reaches its `fastchess` invocation | the flag works and **fired immediately**, which is a finding and not a false alarm. Measured over 400 late-game positions at depth 8 with `stockfish` at 300000 nodes as the truth: 31 `info` lines carrying a mate score, **2 with a principal variation shorter than the distance claimed, 0 with a wrong distance**. The score is right and the line stops at the iteration depth, because a mate found inside quiescence continues below the deepest main-search ply the PV table can hold. Reporting defect, not a search defect, so it did not jump the queue: it is S147, and MANUAL.md carries it as a known bug until then |
| S142 | the `RfpMinPly` floor of 2 is re-measured before it is declared, not read off S145 | `~/.venv/chess/bin/python adocs/data/S145_rfp_sweep.py floor`, re-run at this commit against the 48 constructed mates with `RfpMaxDepth` held at 15 | **reproduced exactly**, digit for digit against `adocs/data/S145_rfp_sweep.log`: 0 and 1 give 19/48 with mate-in-two 13/16 and delays to 7; 2 and 3 give 24/48 with 16/16 at delay 0; 4 and 5 give 27/48. The sweep now names the two settings the raised bound refuses instead of ending in a python-chess `EngineError`. Measurement, not a guard |
| S141 | a pending step's `touches:` names a file that carries every code symbol its `goal:` names | `tools/plan_prose_check.py --touches`, new, and `test_plan_touches` in the fast suite — the one mode of that script that is registered, because it holds no line numbers and so cannot be shifted red by an unrelated source commit the way `--citations` can. Files are comment-stripped before the match, since a mention in a comment is not a landing site and that is the whole of the S039 case. Four symbol classes only: ALL_CAPS with an underscore, a call written with its parentheses, a `_t` type, and a UCI option name matched against the names `src/search_params.hpp` declares. Two ungated note classes: a symbol in code nowhere, and a directory in `touches:` that already holds files of the symbol's kind | green, **0 flagged over 66 pending files, 4 symbols read, exit 0**. **Observed red on the real defect before the repair**: S039, `LAZY_EVAL_MARGIN` in code at `src/search_params.hpp:164` against a `touches:` naming `src/evaluation.hpp`, exit 1 through `ctest -R test_plan_touches` as well as through the script. Non-vacuity, each reverted: S154's `touches:` cut to `fastchess.sh` → `TOUCHES RfpMinPly`; the same cut to `adocs/testing.md` did **not** flag, because that file genuinely names `RfpMinPly` — the containment test is real and not a rubber stamp. The directory rule tested both ways on S039: `tools/` → clean pass, because `tools/eval_model.hpp:976` genuinely carries the symbol; `adocs/data/` → note, abstention, and **that is the rule's measured limit rather than a demonstration of it** — `adocs/data/` holds `.hpp` files because S075 dumped six fitted tables there, so it passes the kind test for a C++ constant. Which is why S039's `touches:` names `adocs/data/S039_eval_spread.log` and not the directory |
| S093 | the gravity update keeps every quiet history entry inside `QuietHistoryMax`, at both asymptotes, and the ordering band it defines clears the countermove band above it and the emptiness below it | `tests/test_search.cpp`, suite `search: move ordering state`, case "gravity holds a history entry inside the bound at both asymptotes": 5000 updates at a depth-12 bonus driven **through the helper** and never assigned, because an assertion on a value the case wrote itself passes on an update that cannot reach it. Asserts the closed interval at every intermediate, exact convergence to `+MAX` and `-MAX` (the fixed point is exact and integer truncation does not stop it arriving, so a short asymptote means the decay term is over-applied), the clamp at 100x the bound, and CPW's two stated properties — an unexpected cutoff moves an entry by the whole bonus, an expected one by nothing. The band itself is `tests/test_evaluation.cpp` "the declared history ceiling clears the band above it", S142's case re-targeted to **both** edges: the ceiling against the countermove band, the floor against it too, and `min(capture, killer_0, killer_1, counter) - max >= 100` so the whole interval stands clear — which is where S025's losing captures would arrive. Preconditions extended with `score_move` returning `-declared_max` unmodified, or a `score_move` that clamped the malused half to zero would make the floor assertion vacuous. CLAUDE.md one-way door: the symptom of getting it wrong is a strength regression, not a wrong node count | green. **Red first, against a helper stubbed to the `int16_t` range with no gravity**: `:644 REQUIRE( rising <= max )` — `values: REQUIRE( 8208 <= 8192 )`. The asserts inside the helper are documentation and not enforcement — both configurations build with `-DNDEBUG` — so what holds the bound is this case |
| S093 | the malus is charged to the quiet moves searched before the cutoff and never to the move that caused it | `tests/test_search.cpp`, two cases. On the table: "a quiet cutoff maluses the quiets tried before it" calls `history_on_quiet_cutoff` directly on four distinct butterfly cells and asserts the cutoff cell positive, **all three** tried cells negative — not all but the last, which is the off-by-one Lynx shipped and removed for +12.89 +/- 5.46 (PR #1756) — and the other colour's half untouched. Through the search: "the cutoff move is credited and the quiets before it are charged" drives `negamax` at depth 1 on `6rk/b5pp/7N/8/3N4/8/8/6K1 w - - 0 1`, where only a mate clears beta so the cutoff move is fixed. Position from a tool (DEC-023): python-chess reports `is_valid() True`, `is_check() False`, mating set `[('Nf7#', 'h6f7')]`. Two seeded killers put two king moves in front of Nf7, which is the stated precondition — Nf7 is the first quiet the generator emits, so with a cold table the span would be empty and every assertion vacuous. `Nxg8` is a capture ordered ahead of all three and earns neither, which holds the gate mirror; Black's half is asserted empty, which holds the colour axis. **Lynx PR #610's illegal-move span bug is deliberately not asserted**: `generate_moves()` emits legal moves only (`src/bitboard.cpp:885`) and `make_move` refuses only on a full game-history stack, so the case would hold over an empty set — verified by building the pinned-knight position and finding the generator emits none of its eight illegal moves | green, 20065 assertions over the three cases. **Red first, against a helper applying the bonus and no malus**: `:731 CHECK( ... < 0 )` — `values: CHECK( 0 <  0 )`, three times, one per tried quiet; and `:844`/`:845 CHECK( state.quiet_history[WHITE][g1][g2] < 0 )` — `values: CHECK( 0 <  0 )` |
| S093 | quiet history carried from one `go` to the next is cleared on `ucinewgame` and on a root that does not descend from the last one searched — **the feature was reverted and these tests went with it; the row is the record** | `tests/test_engine.cpp` "ucinewgame clears the quiet history it carries" and "quiet history crosses a go into a descendant and not into a stranger", plus `tests/test_search.cpp` "quiet history is carried only into a descendant of the root it was learned from" over the pure rule's five branches. The integration bound was **derived and not chosen**: at `go depth 1` every child of the root is quiescence, which writes no history, so the root is the only writer and writes at most `MAX_MOVES` entries — and one `go depth 10` fills 439, asserted as the precondition, so the two sides cannot be confused. The stranger is a bare FEN, which is what empties the ancestor chain; the descendant is `position startpos moves ...` with one more move, the exact shape fastchess replays, so the pitfall list's "the keep path must engage there or the verdict measures nothing" became a test rather than an argument | **all green when written, all removed at the revert.** Red first in three stages, because the rule has two directions and one stub cannot show both. Stage 1, the hoist with the old per-`go` clear (byte-identical to `40f5b56` on `search_bench`): `:1311 REQUIRE( uci_quiet_history()->has_last_root )` — `values: REQUIRE( false )`, and `:1355 CHECK( ... > 270 )` — `values: CHECK( 0 >  270 )`, the carry itself. Stage 2, persistence with both guards missing: `:1318 CHECK_EQ( ..., 0 )` — `values: CHECK_EQ( 439, 0 )`, `:1367 CHECK( ... <= 270 )` — `values: CHECK( 460 <= 270 )`, and the rule's three refusing branches. Stage 2b, the rule stubbed to `return false`, for the two keeping branches stage 2 could not reach. Verdict: `Elo -1.65 +/- 4.22` over 15398 games, H0, DEC-101 |
| S160 | with `REF` unset `fastchess.sh` resolves the reference to `HEAD`; each side's banner line carries its own commit's date; a clean-tree A/A is refused however the ref is spelled and leaves nothing on disk; `AA=1` asks for that match on purpose; and a run outside a git checkout still prints a terminal marker | `tests/test_fastchess_script.sh`, cases 3 to 8, in the fast suite. Case 3 modifies a tracked file so the sandbox tree differs from `HEAD`, then greps the banner for `^reference <head-sha>  <date>$`; red against the pre-S160 script, which dies `fatal: Needed a single revision` because the sandbox has no `7b4d9a4`. Case 5 runs `REF=HEAD~1` against a sandbox whose parent commit has `GIT_COMMITTER_DATE` forced to 2020-01-02 and asserts that date on the reference line -- **mutation-checked, not merely green**: cases 1 to 4 all run at ref == HEAD, where `commit_date HEAD` on the reference line is invisible, and that mutant survived the first version of this test. `%cd`->`%ad` is caught by the same case, because only the committer date is forced. Cases 6, 7 and 8 were red against S160 as shipped: an explicit `REF=$(git rev-parse HEAD)` played the full A/A, `AA=1` did not exist, and a run with `.git` removed aborted with no marker at all. Case 4 also asserts no output directory survives a refusal, and its marker grep is pinned to the guard's own sentence rather than to the bare `SPRT-RUN-FAILED` that every `fail()` prints. Case 7 is case 4's positive control: same sandbox, same clean tree, same resolved ref, `AA=1` set, stub invoked
