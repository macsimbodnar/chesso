# Testing ledger

Acceptance criteria with their covering tests. Rows are added together with the
feature, never afterwards. A step cannot complete without a row referencing its
id. Appends only, with one pruning exception (2026-08-13): when the checker
prunes a completed step's plan.md entry — it keeps the newest five completed
entries listed — every ledger row all of whose step ids were pruned in that
pass leaves with it. A row that also names a live step, an invariant or a
decision stays; `plan_done/` keeps the step's stamp and git keeps every pruned
row.

Two kinds of row appear here, and the difference matters. A **test** row names
something in `ctest` that fails on regression. A **measurement** row names a run
that produced a number at a point in time; it does not re-run, and it is
evidence rather than a guard. INV-6 is why measurement rows exist at all: a
change that alters play is retained only against a verdict, and a verdict of
zero is recorded as zero.

Rows S001 to S016 were written retrospectively when moltke was adopted
(DEC-017). Their measurements are transcribed from the commits and the plan
documents they replace; their test columns name tests that exist today.

| Step | Criterion | Covering test | Result |
|---|---|---|---|
| INV-1 | legal-only generation, perft exact against an oracle | `test_movegen` "shallow perft matches every column"; `test_perft` (label `slow`); `bench_movegen` verifies counts before printing | green |
| INV-2 | make/unmake are exact inverses | `test_engine` "hash and board survive make/unmake"; debug build asserts `squares[]` against the bitboards on every make and unmake | green |
| INV-3 | captures and quiets partition the full list | `test_movegen` "captures and quiets partition the list", 90.5 M assertions over a three-ply tree from every test FEN | green |
| INV-4 | accumulators equal a full recomputation | `assert(eval_accumulators_match(...))` at three sites in `src/bitboard.cpp`, debug build only | green |
| INV-5 | `evaluate()` is side-to-move relative | `test_evaluation` "colour symmetry over every test position", "a mirrored start position is balanced" | green |
| INV-6 | behaviour-neutral changes prove it | `tools/search_bench.py` node counts and best moves, compared by hand per change | procedure, not a test |
| S015 | quiescence pruning measured in games | fastchess | measured, **0 Elo**, kept; rerun outstanding, folded into S022 |
| S018 | the phase that costs most is named by measurement | early middlegame 44.1 cp/move and 36.0 % of loss; endgame 18.3 cp/move and 20.5 % | measured, **contradicts the S016 anecdote**, S019 affected |
| DEC-033 | 16x more search removes only a quarter of the error | `tools/depth_vs_eval.py` over 160 positions: 170.1 cp/move at 4M nodes, 129.1 at 64M, 95 of 160 moves unchanged; evidence in `adocs/data/DEC033_depth_vs_eval.tsv` | measured, **evaluation-limited**, plan reordered |
| DEC-035 | the engine is still evaluation-limited after the fit | `tools/depth_vs_eval.py`, same criteria and sample size as DEC-033: 29.8 % of the error removed by 16x search against 24.1 %, 78 of 160 moves unchanged against 95, 10.4 cp per doubling against 10.3 | measured, **shifted toward search, order not turned over** |
| DEC-035 | the fitted evaluation is no longer systematically optimistic | same profile: mean bias -1.5 / -24.3 / -40.3 / -3.5 / +204.3 by phase, against +39 / +77 / +100 / +42 / +30 before; medians 6 / 0 / -4 / -1 / 227 | measured, **reversed in four phases of five**, MANUAL.md rewritten |
| S028 | the gain shows against a fixed opponent, not only against the previous commit | 120 games achesso vs sgambetto at 10+0.2 under the loose adjudication of DEC-031: 67.5 %, +126.97 +/- 60.30 Elo, against 39.3 % over 210 games under identical settings at S018 | measured, **chained SPRT gains are not drift** |
| DEC-036 | an evaluation term can be priced without the search confounding it | `tests/bench_eval`, ten positions from a full board to bare kings, scores and checksum printed before any timing; 40000000 calls a sweep, resolution 0.1 % over three interleaved passes | green, instrument |
| DEC-036 | recomputed mobility costs what INV-4 exists to prevent | `bench_eval`: 1.31 ns per call against 15.93, 12.2x. `search_bench` depth 12: nps -32.8 % kiwipete, -45.5 % midgame, -23.7 % tactical, wall time +33 % | measured, **probe reverted, nothing shipped** |
| DEC-037 | recomputed mobility is decided by play, not by nodes per second | `REF=c8fe860 ./fastchess.sh --fast`, 10+0.2: **-14.93 +/- 16.44 Elo** over 1164 games, LLR -2.23 crossing -2.20, H0 accepted, LOS 3.72 %, Ptnml [77, 129, 203, 113, 60] | measured, **rejected and reverted**, weights were never fitted |
| S034 | the margin is chosen from data, not from a guess | tapered mobility over 149084 S028 self-play positions: p50 19, p75 34, p90 50, p95 60, p99 81, p99.9 107, max 143 | measured, margin set to 150 |
| S027 | what the fitted weights do to the margin, measured rather than assumed | `build/tools/eval_spread --data .tuning/selfplay_v1.tsv --limit 200000`: combined correction p99 129, p99.9 179, max 289; king safety alone p99 54, max 143; over `LAZY_EVAL_MARGIN` on 797 of 200000 positions, 0.399 %, against mobility's own 231 and 0.116 % | measured, no verdict: the clamp keeps the shortcut sound either way, and S034's margin was chosen from mobility's distribution alone |
| S027 | the tuner emits every parameter it fits, three terms later | 40-epoch smoke run over 1490839 rows writes `pawn_structure_mg` and `pawn_structure_eg` into the pasteable header alongside the other six arrays | green; the writer was extended in the same commit as the model, which is what S034 did not do and cost eight discarded weights |
| S027 | the tuner emits every parameter it fits, four terms later | 40-epoch smoke run over 1490839 rows writes `piece_placement_mg` and `piece_placement_eg` into the pasteable header alongside the other eight arrays | green; the writer was extended in the same commit as the model, which is what S034 did not do and cost eight discarded weights |
| S027 | the tuner emits every parameter it fits, five terms later | 40-epoch smoke run over 1490839 rows writes `tempo_mg` and `tempo_eg` into the pasteable header alongside the other ten arrays | green; the writer was extended in the same commit as the model, which is what S034 did not do and cost eight discarded weights |
| S044 | the owner-run boundary reads DEC-015 as amended by DEC-041 everywhere it is stated | `grep -n "DEC-015\|owner" adocs/plan.md adocs/specs.md` — every workflow hit assigns the owner only S029's network training; procedure, not a test | checked; before the step both files assigned fits and tuning to the owner |
| S045 | S024 scopes continuation history as two tables with a verdict each, distinct from the countermove heuristic | `grep -n "countermove\|one-ply\|two SPRT" adocs/plan_todo/S024_continuation_history.md` — the note names the heuristic as present, history as absent, and the accepts asks one verdict per table; procedure, not a test | checked; before the step the note called the countermove table one-ply continuation history |
| S046 | S030, S031 and S032 each carry an INV-6 discharge in their accepts | `grep -n "search_bench\|INV-6" adocs/plan_todo/S030_move_encoding_16_bit.md adocs/plan_todo/S031_single_side_to_move_key.md adocs/plan_todo/S032_pext_sliding_attacks.md` — all three accepts name search_bench identity, S030 carries the SPRT branch, S031 states the xor construction that makes hashes bit-identical; procedure, not a test | checked; before the step all three stopped at perft, which says nothing about the tree |
| S047 | no pending step or spec line still describes the retired Apple machine as current | `grep -n "cannot be measured here\|Blocked on hardware\|opendirectoryd\|stops being optional" adocs/plan_todo/*.md adocs/specs.md` — the only hit is specs.md's dated 2026-08-13 note recording the replaced text; the 12 % is_check figure carries its Apple condition; procedure, not a test | checked; before the step S032 read blocked, S029 called x86 non-optional, specs priced SPRTs on three Apple cores |
| S049 | S039 cites the decision that governs the lazy shortcut | `grep -n "DEC-" adocs/plan_todo/S039_lazy_margin_redecide.md` — decisions reads DEC-039, DEC-034 appears nowhere; procedure, not a test | checked; before the step it cited the one-night fit delegation, a DEC-034/S034 transposition |
| S050 | S022 asks one verdict per change, and its accepts and body agree | `grep -n "two SPRT verdicts\|one at a time" adocs/plan_todo/S022_delta_pruning_quiescence.md` — the accepts asks two verdicts in either order, the body asks one at a time, no line asks one run to measure two changes; procedure, not a test | checked; before the step the accepts required "the same run" to re-measure S015, which its own body forbade |
| S051 | S020 and S030 complete on a measurement, not on a positive gain | `grep -n "zero is recorded as zero" adocs/plan_todo/S020_single_check_computation.md adocs/plan_todo/S030_move_encoding_16_bit.md` — both gates ask for a number with its noise floor and a keep-or-revert call, S020 names hyperfine over interleaved fixed-depth runs; procedure, not a test | checked; before the step both required a gain larger than the resolution, which the modal honest outcome (DEC-019: 0, 0, slower) cannot satisfy |
| S052 | S021 points at the file where iterative deepening lives | `grep -n "iterative" src/*.cpp adocs/plan_todo/S021_aspiration_windows.md` — the loop is src/chesso.cpp:544 and the touches line says so, agreeing with S037; procedure, not a test | checked; before the step it named src/search.cpp, disagreeing with S037 about the same loop |
| S053 | the ledger's header states the checker's retention rule as the code implements it | prune_plan() in the installed moltke.py:1684-1727: plan.md keeps the newest PLAN_DONE_KEPT=5 completed entries, and a row leaves only when every S-id it mentions was pruned in that pass; the header paragraph states exactly that; procedure, not a test | checked; before the step the header said "Append only" while completions had pruned 84 rows the same day |
| S040 | the section's step attributions name the steps that added the parameters | mobility at S034 from `adocs/plan_done/S034_evaluation_call_cost.md:3,197`; king safety, passed pawns, pawn structure, piece placement and tempo at S027 from `adocs/plan_done/S027_handcrafted_eval_terms.md:2,163,172` and `adocs/specs.md:72`; procedure, not a test | checked, **unchanged**: `tools/eval_model.hpp:52`, `tools/tuner.cpp:8`, `:628` and `:669` date passed pawns to S035, which is `S035_fastchess_script_repair` and has nothing to do with the evaluation — the plan record wins on what a step id names, and the four code comments are a defect outside this step's `excludes:` |
| S041 | every `--only` group frees at least one parameter, the groups are pairwise disjoint, and their union is exactly `[0, PARAM_COUNT)` | `test_tuner_groups` "the --only groups partition the parameter vector": group names parsed out of `tuner_groups::GROUP_LIST`, `all` excluded as the whole vector, length and coverage taken from `eval_model::PARAM_COUNT`. Preconditions asserted first in "the group list and the parameter vector are what the code says" — `PARAM_COUNT > 0`, at least two names, exactly one `all`, every listed name accepted by `free_mask`, `not_a_group` refused, and `TEMPO_EG_BASE + TEMPO_COUNT == PARAM_COUNT` so the bases chain agrees with `eval_model.hpp`'s independent width sum | green, 45 assertions, 0.00 s, label `fast`; observed red three ways — the fourth recorded swallowing (`piece_placement` re-ended at `PARAM_COUNT`) at `CHECK( 2 == 0 )`, "2 of 827 parameters are freed by more than one --only group; the first is index 825, claimed by piece_placement and tempo"; `tempo` dropped from `GROUP_LIST` at `CHECK( 2 == 0 )`, "2 of 827 parameters are in no --only group; the first is index 825"; `material` emptied at `CHECK( 0 > 0 )`, "group frees no parameter at all: material" |
| S041 | splitting `free_mask` out of `tools/tuner.cpp` into `tools/tuner_groups.hpp` leaves the tuner's behaviour identical | two comparisons against `fcd82f8`. (1) `free_mask` itself: `HEAD`'s own text compiled into a scratchpad translation unit and asked for all 9 groups plus 8 names it must refuse, dumping return value, mask length and all 827 bits per call — 8160 bytes over 19 lines, byte-identical to the same dump through the new header. The move is textual: `diff` of the moved region against `git show HEAD:tools/tuner.cpp` differs on exactly two lines, `const char* const` -> `inline constexpr const char*` and `bool` -> `inline bool`. (2) end to end: `tuner --only <group>` for all 9 groups over a 2-row TSV at `--k 1 --epochs 1 --report 1 --validation 0.5 --threads 1 --seed 1`, `HEAD`-built binary against the new one — emitted header and stderr identical for every group after normalising the `--out` filename, the 9 headers being 9 distinct files. `--help` and the unknown-group error identical too; procedure, not a test | measured, **identical**; the split moved code and changed no line of the body |
| S041 | the S040 rows that cite `free_mask` still point at the file it lives in | S041's move invalidated three coordinates S040 recorded one commit earlier: `GROUP_LIST` and the four `free_mask` comments are `tools/tuner_groups.hpp:37-39` and `:70-72`, `:76-78`, `:82-84`, `:88-90`, not `tools/tuner.cpp:153-155` and `:186-189`, `:192-194`, `:198-200`, `:204-206`; `tempo` is `:93-95`, not `:209-211`; and removing the 71-line region shifted the rest of `tuner.cpp` up by 66, so `write_tables()` is `:524-695`, not `:590-761`. The rows above are dated evidence and are not rewritten; this row is where the new coordinates live; procedure, not a test | checked, **relocated**; rows 61 to 63 would otherwise send a reader to a file that no longer contains the symbol |
| S054 | a misformatted **untracked** source file makes `./clang-format.sh --check` exit non-zero, a tracked one still does, and one under a gitignored path is still skipped | `test_clang_format_script`, label `fast`, 0.25 s, five assertions in a throwaway git repository holding the repository's own `.clang-format`. Preconditions first — a sandbox whose only source is formatted exits 0, so a script that always fails cannot pass, and a misformatted **tracked** file exits non-zero **and is named in the output**, so the fixture text really is misformatted under this style. Then the defect (untracked, named in the output), the negative half (`build/generated.cpp` and `.ref-builds/copy.hpp` misformatted, exit 0), and the deleted-file guard, itself preceded by asserting the index still names the missing file. A final check that 5 sandboxes were built, so a skipped case is not a pass | green; observed red with `clang-format.sh:60` reverted to `git ls-files` in place and run through `ctest -R test_clang_format_script`, verbatim: `FAIL: a misformatted UNTRACKED source file passed the check (exit 0); the file selection lists the index only` over an empty `out.txt`, and `FAIL: a file deleted from the worktree but still in the index was handed to clang-format (exit 1)` over `src/gone.cpp: No such file or directory`. The three preconditions and the gitignored half passed in the red run, which is what makes those two failures about the selection |
| S054 | gitignored trees stay excluded — `build*/` and `.ref-builds/` are not scanned | file counts at `d853843` + the fix: `git ls-files \| grep -E '\.(c\|cc\|cpp\|h\|hpp\|hh)$'` is 42 and `git ls-files --cached --others --exclude-standard \| grep …` is also 42 with no untracked sources present, so the fix adds nothing on a clean tree. With two untracked probes written, 42 against 44, and `comm -13` names the delta as exactly those two files. Paths in the new selection matching `^(build\|\.ref-builds\|\.no_git)`: **0**. Dropping `--exclude-standard` adds **7**, all named: the CMake compiler-id probes under `build/`, `build-debug/` and `build-prof/`, and `.no_git/search_old.cpp`; procedure, not a test | measured, **excluded**; `.ref-builds/`'s 32 sources on disk never appear even without `--exclude-standard`, because `.ref-builds/7b4d9a4/.git` is a `gitdir:` pointer into `.git/worktrees/` and `git ls-files --others` does not descend into a nested repository — held out twice over, and the sandbox's plain `.ref-builds/` directory is what tests the `.gitignore` mechanism itself |
| S054 | `--cached` names files deleted from the worktree, which clang-format cannot open | at `d853843` + the fix, with `src/zz_probe_del.cpp` added to the index and then removed from the worktree: the old selection hands it to clang-format, which prints `src/zz_probe_del.cpp: No such file or directory` and exits 1; the fixed script exits 0. Asserted by `test_clang_format_script`'s fifth case; procedure plus test | fixed in the same change; the old line had the same weakness and never met the case |
| S054 | the test leaves the working tree exactly as it found it | `git status --porcelain` captured before and after `ctest --test-dir build -L fast`, `diff` of the two: identical, five entries either way. Leftover `${TMPDIR}/chesso-clang-format.*` roots after a green run: 0; after a run with two failing assertions: 0; procedure, not a test | checked; the first draft leaked every sandbox — `sandboxes+=("$tmp")` ran inside the `$(make_sandbox)` command substitution, so the parent's array was empty and the cleanup loop removed nothing. Replaced with one root plus a `trap … EXIT`, which also survives an early exit |
| S038 | the tolerance and its comment agree with the number of integer divisions `evaluate()` actually performs | `test_eval_model` "the model reproduces evaluate() on every phase", tolerance 3.0. Re-derived from the code, not from the audit: `grep -n GAME_PHASE_MAX src/evaluation.cpp` finds **four** divisions in the `evaluate()` path — `:640-643` psqt plus pawn terms, `:668-670` tempo, `:951-952` mobility, `:953-954` king safety — against the two the old comment claimed and the three F04 counted. `GAME_PHASE_MAX` is defined once, `src/evaluation.hpp:289`, so there is no fifth. Three can round today: `tempo_mg == tempo_eg == 0` (`src/evaluation.cpp:582-583`) makes `:668` truncate `0 / 24` exactly, bound 3 x 23/24 = **2.875**; over all four it is 4 x 23/24 = 3.833 | green; **the audit's count was wrong and its bound was right** — F04 missed the tempo division that `src/evaluation.cpp:647-670` documents at length, and 2.875 is the bound only because tempo ships at zero |
| S038 | the corpus includes the FENs the audit finding recorded, and they still exceed the tolerance it was raised from | `test_eval_model` "the pinned positions reach the truncation bound": each of the four F04 FENs must differ from the model by more than the old 2.0, and the worst by more than 2.8. Observed here, model against engine, matching F04's recorded `DRIFT` values to the digit: **2.875** `2r1r1k1/4Q1p1/p1P1p1q1/3p3p/1P1PpP2/4P2P/PB4P1/R4RK1 w - - 5 29`, **2.33333** `6k1/6p1/p7/2R5/2P3n1/P1N3P1/1rP4r/2R3K1 w - - 0 29`, **2.25** `r1bqkb1r/1pp1pp1p/5n2/p2p4/3P3R/2N2N2/PP1PPPP1/R1BQKB2 b Qkq - 2 8`, **2.125** `1k5r/pp3p2/5P2/q1pr4/4R2p/3B3P/P1P2QP1/1R4K1 w - - 6 26`. The case also asserts the tempo weights are zero, the premise that makes the bound 2.875 rather than 3.833 | green, 17 cases and 2604 assertions, 0.01 s, label `fast`; observed red with the four FENs added and the tolerance still 2.0, verbatim four failures `CHECK( 2.875 <= 2 )`, `CHECK( 2.33333 <= 2 )`, `CHECK( 2.25 <= 2 )`, `CHECK( 2.125 <= 2 )`, each logging its FEN. The 26 pre-existing positions all passed in that red run, which is what makes the failures about the pinned corpus rather than about a tolerance nothing could satisfy |
| S038 | the guard is made honest without altering play, so no SPRT is owed | `git diff --quiet 7068cd2 -- src/evaluation.cpp tools/eval_model.hpp` exits 0, and `sha256sum` agrees either side: `18ab9550…5323` for `src/evaluation.cpp`, `b3a96b65…5335` for `tools/eval_model.hpp`. Only `tests/test_eval_model.cpp` changed, so `evaluate()` is bit-identical and INV-6 has nothing to discharge; procedure, not a test | checked, **identical**; the one-division merge that would alter play is S055, after S042 and before S029, with its verdict expected at 0 (DEC-053) |
| DEC-055 | `--allow-tactical` is inert at its default, so the S028 filter stays exactly reachable | `build/tools/datagen --games 240 --nodes 100000 --threads 12 --seed 20260814` through the pre-change binary and the post-change binary. Both print `240 games, 17593 positions`, and `sort \| md5sum` of the two outputs is `2a13141f0b86f333684df5b2be333104` either way. Sorting is required and is not a weakening: workers append under one mutex per finished game, so the row *order* across threads is not deterministic between runs while the row *set* is; procedure, not a test | measured, **identical**; the flag defaults to 0 and the counters read the same booleans the filter already computed |
| DEC-055 | each datagen filter clause has a measured marginal cost, not an argument | `tools/datagen.cpp` prints one line per run counting, per clause, the positions that would have been recorded but for that clause alone. 240 games at seed 20260814, defaults: `27839 considered, 17593 recorded; skipped in-check 1698, tactical best move 4814, mate 0, score past 1000 1824`. The tactical clause is the largest at 17.3 % of considered, +27.4 % on top of what is recorded | measured, **the loosened clause is the one the code contradicts**: `src/search.cpp:125` calls `evaluate_lazy()` at the top of every quiescence node, before `generate_captures` at `:152`, and returns on that stand-pat score at `:136-137` |
| DEC-055 | the score cap is kept on a measurement, not on taste | same 240 games at `--quiet-limit 1000000 --allow-tactical 1`: 24603 rows, 2196 of them (8.93 %) past 1000. Weighting each row by `sig * (1 - sig)` as `tools/tuner.cpp:380-384` does, at the K this corpus fits (0.7472), those rows carry **0.206 %** of the corpus's gradient mass; the factor is 0.01319 at a score of 1000 against 0.25 at an equal position, 5.28 % | measured, **near-inert, cap kept at 1000**; the same run shows the mate clause is shadowed rather than redundant — 0 marginal rejections under the cap, 649 without it |
| S065 | what `datagen` writes is what `tools/tuner.cpp` parses, field by field | 56304-row smoke corpus audited with `awk -F'\t'`: every row exactly **4** tab fields, field 1 exactly **6** space-separated FEN fields, `result` only in {0.0, 0.5, 1.0}, `phase` inside [0, 24], `score` inside [-999, 999]. Against `load()` at `tools/tuner.cpp:171-204`, which needs four `getline`s on `'\t'`, then `placement` and `side_to_move` off field 1 by `>>`, `0 <= atof(result) <= 1` and `0 <= atoi(phase) <= GAME_PHASE_MAX`; field 3 is consumed and never converted. End to end: `build/tools/tuner --data … --epochs 20` read all 56304 rows, fitted K = 0.7472 and descended 0.120776 to 0.109413 held out, exit 0, no `bad row` and no `bad placement` | measured, **parses**; F02's trap is the other direction — `eval_spread` reads the FEN as tab field 1, which a raw file does not satisfy |
| S065 | the eight-hour corpus size is extrapolated from a measured rate, not guessed | 600 games at `--nodes 100000 --threads 12 --allow-tactical 1`: 56304 positions in 143.93 s. 4.169 games/s and 391.2 positions/s, so 28800 s gives 120067 games and 11.27 M positions; 120000 x 93.84 positions per game = 11.26 M cross-checks it. 64.15 B/row = 723 MB against 34 G free; the tuner's 10.9 MB resident at 56304 rows scales to about 2.2 GB against 9 GB available, and 2.22 ms/epoch to about 0.44 s/epoch, so S028's 11200 epochs cost roughly 1 h 23 m | measured, **120000 games**; S028's corpus was 1490839 positions from 20000 games, so 7.6x the rows and 6.0x the independent games |
| S066 | nearly every game lands on both sides of the tuner's validation split | `tools/tuner.cpp:795-805` shuffles a **row** index and takes the last 10 %; `tools/datagen.cpp` writes a game's rows inside one `lock_guard`, so they are contiguous. Over the 56304-row smoke corpus, descents in the FEN's fullmove number give **600 contiguous runs for 600 games**, one per game exactly. Simulating the split over the real per-game row counts: 599, 599 and 597 of 600 games straddle it — **99.5 to 99.8 %**. Analytically `0.9^93.84` = 5.1e-05, and 3.9e-04 at S028's 74.5 rows per game | measured, **the held-out set is not held out**; it changes a diagnostic and no game, which is why S066 is minutes and sits ahead of S065's fit rather than behind it |
| S066 | no game lands on both sides of the tuner's validation split, asserted over the splitter rather than by inspection | `test_tuner_split` "no game lands on both sides of the game-level split": a 30-game fixture written to the temp directory, read back through `tuner_split::row_ply`, split at seeds 1, 2 and 20260814, ground-truth game per row held by the fixture. Each seed also asserts the index is a permutation of `[0, rows)`, so a splitter that dropped rows could not pass by holding out less. Preconditions in the two cases before it — the fixture's smallest game has at least 2 rows, no row fails to advance the ply inside a game, and a **row-level** split of the same fixture cuts at least half of its games | green, 10 cases, 6513 assertions, 0.01 s, label `fast`; observed red with `game_starts()`'s predicate forced to `true`, which case 6 proves is exactly the pre-S066 row-level split, verbatim `CHECK( 30 == 0 )` three times over `30 games straddle the game-level split at seed 1`, `... seed 2`, `... seed 20260814`, plus `CHECK( 2106 == 29 )` on the block count, `CHECK( 0 == 1 )` on the two merged games landing together and `REQUIRE( 1 >  1 )` on the largest block |
| S066 | the test is non-vacuous by construction: it fails when its own fixture could not straddle anything | same file, cases 1, 2, 4 and 5. Observed red with the fixture's games shortened to one row each, verbatim `CHECK( 1 >= 2 )` over `the fixture's smallest game has 1 rows; a corpus of one-row games cannot straddle anything`, `CHECK( 0 >= 15 )` over `only 0 of 30 games straddle a row-level split; the fixture is too small or its games too short for the next case to mean anything`, `REQUIRE( 22 == 1 )` and `REQUIRE( 2 == 1 )` on the two joins the fixture is built to contain | green; the guard fires on the fixture, not on the splitter, which is the case a passing one-row corpus would have hidden |
| S066 | the game boundary is reconstructed reliably on the real corpus, and the failure mode is measured rather than assumed | scratch pass over all 11003693 rows of `.tuning/selfplay_v2.tsv`, 0 unparsed, against the 120000 games `datagen`'s own log reports. Blocks found: **ply does not advance 119998** (at most 2 missed boundaries, 0.0017 %), FEN move number descends 119852 (at most 148, 0.1233 %), ply or a piece appeared 119998, ply or castling back or the label changed 119999 (at most 1, 0.0008 %). At most, because a game whose every position was filtered writes no rows and is indistinguishable from a merge. Block lengths under the ply: min 1, max 392 against `--max-plies 400`, mean 91.70 | measured, **the ply is the detector, DEC-056**; every predicate is a within-game invariant, so none can split a game, and a missed boundary merges two games into one indivisible block. Confirmed by the tuner itself: `119998 games` on its first line over the same corpus |
| S066 | games straddling the split, before and after, on the real corpus | same pass, seed 1, `--validation 0.10`, the finest sound partition above as the truth the file does not carry. Old row-level split: **119360 of 119999 games straddle, 99.467 %**. New block split: **0 of 119999**. Realised held-out rows 1100388 against the 1100369 asked for, **10.0002 %** against 10 %, an overshoot of 19 rows bounded by the last block taken | measured, **zero, and zero by construction**; the smoke-corpus simulation in the step file put it at 99.5 to 99.8 %, and the real corpus agrees at 99.47 % |
| S066 | the change moves which rows are held out and nothing the fit computes, by inspection of the code | `evaluate_position`, `sigmoid`, `error_range`, `gradient`, `fit_k` and the Adam loop extracted from `dd57c3c:tools/tuner.cpp` and from the working file and hashed: all six **byte-identical**, 33, 2, 40, 134, 28 and 46 lines. The 10 diff hunks are the include, `dataset_t::ply`, `load()`'s `row_ply` call and its push, `write_tables()`'s signature and one header line, two `usage()` help lines, and `main()`'s split construction; procedure, not a test | checked, **identical**; nothing between the training rows and the emitted numbers changed |
| S066 | the change moves which rows are held out and nothing the fit computes, end to end | the first 200000 rows of `selfplay_v2.tsv` with **only** the FEN move number rewritten to a strictly descending counter — asserted field by field, every other FEN field and every other column untouched — so every row is its own game and the new splitter's index is provably the old permutation. `--k 0.7615 --epochs 300 --report 50 --validation 0.1 --threads 12 --seed 1` through the `dd57c3c` binary and the new one: `180000 train, 20000 validation` both, every one of the six epoch reports identical to all six digits, `best: train 0.114045  validation 0.113803  (start 0.125481 / 0.125367)` both, and the 827 emitted constants byte-identical at sha256 `f710eaff…5399`. The only stderr difference is the first line, which now also carries the game count and the realised fraction; the only header difference is the new `// split      by game, S066` line | measured, **identical**; the fit sees the same rows in the same order and returns the same numbers |
| S066 | the S065 corpus fitted under both splitters, held-out figures side by side | `.tuning/selfplay_v2.tsv`, 11003693 rows, `--k 0.7615` pinned to the whole-corpus fit so the objective is identical on both sides, `--seed 1 --epochs 3000 --report 100 --patience 1000000 --threads 12`. Row level: 9903324 train, 1100369 held out, start 0.122699 / 0.122933, best **train 0.117053 validation 0.117352**. Game level: 119998 games, 9903305 train, 1100388 held out, start 0.122741 / 0.122560, best **train 0.117059 validation 0.117380**. Held-out difference **+0.000028**; generalisation gap (held out minus train) +0.000299 against +0.000321, widened by 0.000022 | measured, **no difference that can be attributed**, recorded as zero: the two held-out figures differ by 2.8e-05 while the two held-out **sets** already differ by 3.7e-04 at the untuned starting constants, thirteen times as much. 827 parameters over 9.9 M rows cannot memorise a game. The splitter is kept because the old instrument was wrong by construction, not because a number moved |
| S066 | the block-ordered index is not slower than the shuffled one | same two runs, same 3000 epochs, same corpus, machine otherwise idle: **1090.87 s game level against 2333.91 s row level, 2.14x**. Peak RSS 1227876 KB against 1140956 KB, +7.6 %, which is the 44 MB `ply` vector and the index built by `reserve` | measured, **2.14x faster**; a fully shuffled row index random-walks a 1.2 GB dataset every epoch while blocks walk it in near-file order. Not why the change was made and not load-bearing for anything |
| S066 | a corpus too small to hold a game out keeps its rows for training instead of producing `-nan` | `test_tuner_split` "the last block is never held out": 1, 2 and 3 games of 1, 2 and 40 rows at `--validation` 0.1, 0.5, 0.9, 0.99 and 1.0. Three exact assertions per combination — one whole block always survives, a one-game corpus keeps every row, and at `--validation 1.0` exactly one block survives and no more — each over a corpus whose block count and block length are asserted first. End to end: `build/tools/tuner` over a two-row single-game corpus at `--validation 0.5` now prints `2 train, 0 validation (0.0000%)`, then `WARNING: 1 game(s) in this corpus, so nothing could be held out`, then refuses the fit | green, 10 cases, 6513 assertions, 0.01 s, label `fast`; **the defect was introduced by this step and caught by its own test**. Before the clamp the same invocation printed `0 train, 2 validation (100.0000%)` and `epoch 1  train 0.000000  validation -nan`. Observed red with the clamp reverted to `first_held > 0`, verbatim `CHECK( 0 >= 1 )` over `fewer than one whole block left to train on: 0 rows of 1, 1 games at --validation 1`, `CHECK( 0 == 1 )` over `a one-game corpus held out 1 of its 1 rows`, and `CHECK( 0 == 2 )` over `at --validation 1.0 the clamp kept 0 rows rather than one block of 2`. A row-level split could always cut one row off a game, so no earlier version could meet this case |
| S066 | the test leaves the working tree exactly as it found it | `git status --porcelain` captured before and after `ctest --test-dir build -L fast`, `diff` of the two identical, nine entries either way. The fixture is written to `std::filesystem::temp_directory_path()`, read back, and removed inside the builder, so it exists only for the duration of one read and no case depends on another having cleaned up; case 1 asserts its absence afterwards. `ls /tmp/chesso_test_tuner_split.tsv` after a green run: `No such file or directory` | checked, **unchanged**; the fixture never enters the repository, so `.gitignore` is not what keeps it out |
| S065 | a fit over the regenerated corpus reduces held-out error below the constants the engine ships, with no refusal warning | `build/tools/tuner --data .tuning/selfplay_v2.tsv --out .tuning/tuned_v2.hpp --threads 12 --epochs 5000`, everything else default (`--lr 1.0 --report 100 --patience 20 --validation 0.1 --seed 1`): 11003693 positions, 119998 games, 9903305 train, 1100388 held out (10.0002 %), 827 parameters all free, **K = 0.7624** fitted from the train rows and then held. Start train 0.122741 / held out 0.122560; best **train 0.117043 / held out 0.117359, at epoch 4800**, no refusal warning, 2082 s wall on 12 threads. Curve: 0.118022 at 100 epochs, 0.117532 at 1000, 0.117424 at 2000, 0.117380 at 3000, 0.117366 at 4000, 0.117359 at 4800 — five sixths of the total gain inside the first 1000 epochs. Emitted header sha256 `b60e7134…40bc4` | measured, **descends by 4.24 % of the starting held-out error**; **not comparable with S028's 0.113852 → 0.108043** — different corpus, different objective, K refitted per corpus (0.7624 against 1.1141), and S028's figures were taken under the row-level split S066 replaced. Only the SPRT decides, and it has not run |
| S065 | the epoch budget is chosen from the held-out curve rather than from the clock | a first run at the default `--epochs 20000` was killed by the harness at epoch 8500, having reached held out **0.117357** — 2e-06 below the 4800 best over 3700 further epochs, on a curve already flat to the sixth decimal from epoch 4000. The 5000-epoch re-run's log is **identical to that run's, line for line, through epoch 5000** (53 lines, `diff` clean), so the fit is deterministic at a given seed and thread count and a shorter budget traces the same curve rather than a different one | measured, **flat**; the tuner keeps the best-held-out checkpoint, so a longer budget could only have bought that 2e-06 |
| S065 | every one of the 827 fitted numbers reaches the two source files, so a half-applied fit cannot look like a fit that did not work | scratch verifier re-extracting the weights from `src/eval_tables.hpp` and `src/evaluation.cpp` after the paste and comparing them one by one against the emitted header: **827 compared, 827 identical** — 5 defines, 768 psqt, mobility 8, king safety 18, passed pawns 12, pawn structure 6, piece placement 8, tempo 2. `git diff --stat` was 111 insertions and 111 deletions across the two files, values only; `./clang-format.sh` reflowed the two king safety arrays and `--check` was clean. Non-vacuous: with `tempo_mg` and one `mobility_mg` entry altered in a copy of the header it reported 825 identical and named both arrays; procedure, not a test | measured, **complete**, then reverted — see the next row |
| S065 | applying the fitted constants fires three guards, each with the arithmetic that decides it | `cmake --build build -j12 && ctest --test-dir build -L fast` at the paste: **9 of 12 green, 3 red**, `test_evaluation`, `test_search`, `test_eval_model`. (1) `tempo_mg = 39, tempo_eg = 21`, so `test_eval_model`'s `CHECK(tempo_unfitted)` fails with its own message: all four taperings can truncate, the bound is 4 x 23/24 = **3.833** and the tolerance has to go to 4 — DEC-053's stated consequence, arrived at by fitting tempo for the first time. (2) the same case's four pinned FENs, whose residuals belong to the old weights, now differ by **2.541667 / 1.250000 / 1.125000 / 2.416667** against 2.875 / 2.333333 / 2.25 / 2.125, so two fall under `> 2.0` and the worst under `> 2.8`. (3) `test_evaluation` "removing a piece moves the score": a white queen on d1 against bare kings is worth **713** where her own fitted material value is 1152, off by **439** against `POSITIONAL_ROOM` 150 — decomposed as -354 from `psqt_eg[queen][d1]` at phase 4, -116 from `mobility_eg[queen]` over 17 squares, +28 king safety | measured, **the paste is reverted and no SPRT was run**. (1) is arithmetic DEC-053 already decided; (2) and (3) are guards whose thresholds are evidence of the old weights, and re-targeting them is a decision rather than this step's business. S056 does **not** unblock it: its `touches:` is `adocs/plan_todo/S055_taper_stage_two_once.md` and its `excludes:` forbids editing `tests/test_eval_model.cpp` |
| S065 | the value anchors the paste moves are re-derived independently of the engine rather than read off it | a second implementation of `evaluate()` over seven positions, written from `src/evaluation.cpp`'s specification — the four tapered divisions truncating towards zero at `:640`, `:668`, `:951`, `:953`, the `LAZY_EVAL_MARGIN` clamp at `:984`, the side-to-move sign at `:672` — with every feature count hand-derived from the placement. Against the weights the engine ships it reproduces **8 of 8** pinned values: `test_evaluation`'s 88, 240, 325, 530, 1101, 0 and `test_search`'s 530 with its cheap 524 and its 266. Against the fitted header the same seven positions give 148, 237, 283, 515 (cheap 536), 734, **21** and 251 — every one of them what the engine printed at the paste. 21 is the bare-kings case: the board terms still cancel and what is left is tempo at phase 0 | measured, **agrees on both weight sets**; an anchor copied from the thing it anchors asserts nothing (S028). `test_search`'s two -491 cases moved to -454 (a quiescence result) and -503 (a static evaluation after a played sequence) and were **not** re-derived here |
| S065 | the corpus barely constrains the endgame end of the queen's table, which is where the fit moved furthest | one pass over all 11003693 rows, reading the `phase` column and the placement field only: mean phase **13.06**; `phase <= 4` on 2119786 rows (19.264 %), `phase <= 8` on 4341585 (39.46 %), a queen on the board anywhere on 5512057 (50.1 %) — but **a queen at `phase <= 4` on 9786 rows, 0.0889 %** of the corpus. What moved there: `psqt_eg[queen]` by a mean of **-324** (d1 -46 → -490, g2 -120 → -597) against `psqt_mg[queen]` **+270** and `QUEEN` 1067 → 1152, with `mobility_eg[queen]` 3 → **-9** | measured, **data and not a chess judgement**: `phase <= 4` with a queen is one queen and no other piece, and those squares are fitted from about ten thousand rows in eleven million. The guard in the row above is what noticed |
| S065 | the fitted constants are decided by SPRT, not by the held-out figure | `REF=a2f0065 CONCURRENCY=12 ./fastchess.sh` — the working tree against the constants shipping at `a2f0065`, elo0=0 elo1=5 alpha=beta=0.05, 12 concurrency (DEC-050) | **outstanding, and nothing is measured yet**: the paste was reverted because three guards fired, so there is no candidate to play. 827 constants move at once, which is one change by SPRT's standard and 827 numbers by any other; a verdict of zero is recorded as zero (DEC-019, INV-6) |
