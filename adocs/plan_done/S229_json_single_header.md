id:         S229
goal:       the nlohmann/json dependency is carried as its MIT single header rather than as a gitlink to the whole repository, so a checkout of this branch contains no GPL-licensed file
accepts:    `tests/json` is no longer a submodule (`git submodule deinit`, `git rm`, the `.gitmodules` entry gone, `.git/modules/tests/json` gone); `tests/third_party/nlohmann/json.hpp` (the amalgamated single header of the pinned release, its version recorded) and `LICENSE.MIT` beside it are tracked, and a `THIRD_PARTY.md` in that directory names the project, the version, the URL and the licence; every include path that named `json/single_include/nlohmann` names the new directory (`tests/CMakeLists.txt`, `tools/CMakeLists.txt` and any other); `git grep -n 'GPL' -- ':!adocs/plan_done' ':!adocs/audit' ':!adocs/decisions.md' ':!adocs/status.md'` returns only the founding-rule sentences that mention the licence by name; `books/fetch_book.sh`'s submodule sentence names doctest alone and says how json is carried; the fast suite is green in both builds from a fresh configure; no `src/` change, `No functional change`
touches:    .gitmodules, tests/json, tests/third_party/, tests/CMakeLists.txt, tools/CMakeLists.txt, books/fetch_book.sh, DEV_MANUAL.md, clang-format.sh, tests/test_clang_format_script.sh, tools/mutation_check.py
excludes:   doctest, which stays a submodule (plain MIT throughout); any change to what the tests or tools do with JSON
decisions:  DEC-207, DEC-201, DEC-104
closes:
blocks:
paused_by:
author:     an Opus 5 subagent briefed by the coordinator (DEC-185, DEC-199); started 2026-09-14 15:48, filler while S091 waits for its SPRT night and S222 for its SPSA night
done:       2026-09-14 16:05. **nlohmann/json is a committed MIT single header and no submodule.** **Vendored from the checkout on disk, not from the network**: `single_include/nlohmann/json.hpp` of the pinned submodule copied to `tests/third_party/nlohmann/json.hpp`, sha256 `9bea4c8066ef4a1c206b2be5a36302f8926f7fdc6087af5d20b417d0cf103ea6` before and after, `cmp` silent, 919975 bytes, version macros 3, 11, 3; the library's `LICENSE.MIT` copied beside it, sha256 `86b998c792894ccb911a1cb7994f7a9652894e7a094c0b5e45be2f553f45cf14`, also `cmp` silent. `json_fwd.hpp` was **not** taken: `git grep json_fwd` returns nothing, so nothing here includes it. `THIRD_PARTY.md` beside them names the project, version 3.11.3, the commit `9cca280a4d0ccf0c08f47a99aa71d1b0e52f8d03` that `git describe --tags` calls `v3.11.3`, the URL, MIT with the licence file beside it, the header's sha256 in full, that it is the library's own amalgamated release artefact and unmodified, and that this project's tests are its only consumer. It adds one checkable fact: all 45 `SPDX-License-Identifier` tags in the file read MIT -- Hedley's block included, marked with Evan Nemerson's copyright beside Niels Lohmann's -- so the two `grep -c` forms both answer 45. **Submodule removal**: `git submodule deinit -f tests/json` cleared the directory and unregistered the path, `git rm -f tests/json` removed the gitlink and took the `[submodule "tests/json"]` entry out of `.gitmodules` in the same call, and `.git/modules/tests/json` was deleted. `.gitmodules` now holds `tests/doctest` alone; `.git/config` keeps only `submodule.tests/doctest.active` and `.url`; `ls tests/json` fails. **Include paths**: the two `target_include_directories` lines in `tests/CMakeLists.txt` -- one inside `add_doctest_target`, one beside the `test_perft` target -- name `third_party/nlohmann` instead of `json/single_include/nlohmann`, `SYSTEM PRIVATE` kept, so the three includers compile unchanged. There are **three**, not the two the brief named: `tests/test_chesso.cpp` and `tests/test_helpers.hpp` take `<json.hpp>`, `tests/test_perft.cpp` takes `"json.hpp"`, and because nearly every doctest file includes the helpers, the function's line is what carries the whole suite. `tools/CMakeLists.txt` names no json and is untouched; `.vscode/c_cpp_properties.json` globs the workspace and needed nothing. **One discovery, in scope, fixed here**: `clang-format.sh` would have formatted the vendored header. `git ls-files` names a gitlink as one extensionless entry, so while json was a submodule the selection never saw it; a committed 900 kB `.hpp` it sees, and `--check` is in the gate, so the step would have made its own THIRD_PARTY.md false at the first `./clang-format.sh` run. `tests/third_party/` is now excluded beside `adocs/`, for the reason written next to it, and `tests/test_clang_format_script.sh` grew a seventh case over a tracked and an untracked misformatted file under that path, with the sandbox count raised to 7. Non-vacuous, observed both ways: run against `HEAD`'s copy of the script -- the test takes the script as an argument for exactly this -- case 7 fails and the other six pass; run against the edited script, all seven pass. **The GPL grep**, `git grep -n 'GPL'` with the four exclusions, leaves seven lines and none of them says a fixture is in a checkout: `CLAUDE.md`'s founding-rule sentence (stays, it is the rule); `src/openings.cpp`'s quotation of the Polyglot format page (stays -- DEC-121's ruling on format constants, and `src/` is out of scope here); `adocs/plan.md`'s dated bb_tables sentence (stays, evidence) and its Open-list entry for this step (the coordinator's line to move); `adocs/data/S091_sprt.sh`'s pre-registration naming S229 as open (stays, evidence -- a run was taken while it was open and said so); and this step file's own goal and accepts, which leave the grep when the file moves to `plan_done`. **Two more the accepts did not foresee**: the vendored header matches twice on *SIGPLAN*, the ACM conference in two Grisu2 references, which is a substring hit in a file that must stay byte-identical and cannot be edited -- `git grep -nw 'GPL'` excludes both and the header carries no whole-word match. `THIRD_PARTY.md` was written without the word on purpose, S227's precedent, while stating the operative fact and pointing at the library's own `.reuse/dep5`. **Docs**: `books/fetch_book.sh`'s bundling list now names one submodule, doctest, and says json is carried as the library's own MIT single header with its licence and THIRD_PARTY.md; the whole GPL-fixture clause is gone. `DEV_MANUAL.md`'s mutation-worktree recipe and the paragraph under it name `tests/doctest` alone, and so does the same recipe in `tools/mutation_check.py`'s module docstring -- the old form would now fail, since `git submodule update --init tests/json` errors on a path `.gitmodules` no longer lists. `TOOLCHAIN.md` checked: no submodule or json mention. `MANUAL.md` checked: it is the UCI surface and needed nothing. **Worktrees**: neither `fastchess.sh` (builds `src/chesso` and nothing else) nor `mutation_check.py` (only prints the recipe) runs a submodule step in code; the fast check found the two that did -- `adocs/data/S154_floor_margin_sweep.py` and `adocs/data/S156_mined_floor_sweep.py` copy the listed submodules out of this checkout into their throwaway worktrees and listed `tests/json`, whose guard would now stop them with a remedy `.gitmodules` no longer offers -- and both list `tests/doctest` alone in this commit, the vendored header being a committed file every worktree carries; `adocs/data/S223_fen_census.py`'s exclusion moved from the dead `tests/json/` path to `tests/third_party/` (the header holds no FEN, so its output is unchanged). `.ref-builds/mut` is left at an older commit with `tests/json` still on disk under its own `.git/worktrees/mut/modules/`, which the `.git/modules/tests/json` deletion did not touch; it is gitignored scratch and the recipe recreates it at HEAD. **Gate, both builds freshly configured**: `rm -rf build build-tune`, then `cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DCHESSO_ARCH=native -DCMAKE_CXX_COMPILER_LAUNCHER=ccache` and the same plus `-DCHESSO_TUNE=ON` for `build-tune`, the values read out of the old caches first. Each new `CMakeCache.txt` is identical to the saved old one, entry for entry (`.tuning/coord/S229/cache_before.txt`, `tune_cache_before.txt`). The TESTS chain exits 0: **39/39 fast in `build` (118.61 s), 39/39 in `build-tune` (120.56 s)**, `./clang-format.sh --check` clean under `CLANG_FORMAT_MAJOR=22` (`.tuning/coord/S229/gate.log`). `test_perft` is not in the fast label and is the one consumer of the quoted include form, so it was built in both and run for real in Release: exit 0, every expected and real column equal (`.tuning/coord/S229/perft.log`). `./build/src/chesso bench | tail -1` prints **5950740 nodes**, the figure the brief named; no `src/` file was touched, so the commit is **No functional change**. **For the owner, `README.md` untouched** (DOCS): its `git submodule update --init --recursive` line is **still correct** -- doctest remains a submodule and the flag is still needed. **For the coordinator**: `adocs/status.md` quotes the old recipe naming `tests/json` in a handover paragraph, and HEAD moved from a07ef98 to 38042ea during this step, which is that handover's own correction commit.

## Why this exists

S227's fast check: `tests/json/.reuse/dep5` declares the library's own test
fixtures under `tests/thirdparty/imapdl/*` as GPL-3.0-only; a
`git submodule update --init` puts them in every checkout although nothing
here builds, includes or ships them. The owner's stated reason for the no-copy
rule is "no GPL question anywhere in this codebase" (CLAUDE.md); the single
header is the library's own MIT distribution form and removes the question
without changing a line of what the tests do. Vendoring a dependency's own
release artefact with its licence is not copying engine code (DEC-104 is about
engines); the DEPS rule is not engaged because no dependency is added.

## Cost

Agent work, under an hour; a fresh configure and the gate. Filler behind S228
(DEC-171: not reachable in play).
