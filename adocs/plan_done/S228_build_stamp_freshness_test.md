id:         S228
goal:       a test proves the `id name` build stamp follows the tree without a reconfigure -- configure once, change the commit or the dirty state, rebuild, and the reply changes -- so S212's identity check cannot be defeated by a stale header (2026-09-12_plan_adversarial-F02, DEC-206)
accepts:    a test in the fast suite (shell or Python, registered in `tests/CMakeLists.txt`) builds a sandbox checkout with a tiny CMake project or drives `cmake/build_info.cmake` as `cmake -P` the way the `chesso_build_info` target does: it generates the header at one commit, commits a change or dirties a tracked file, re-runs the generation step **without reconfiguring**, and asserts the header's sha and `-dirty` suffix moved; a second case asserts an unchanged tree leaves the header byte-identical (no rebuild storm); observed red against a version of the script that captures the sha once; `DEV_MANUAL.md`'s paragraph on the stamp cites the test; no `src/` change
touches:    tests/, tests/CMakeLists.txt, cmake/build_info.cmake, DEV_MANUAL.md
excludes:   the stamp's form (DEC-204); `fastchess.sh`'s identity check itself (S212's properties cover it)
decisions:  DEC-206, DEC-204
closes:     2026-09-12_plan_adversarial-F02
blocks:
paused_by:
author:     an Opus 5 subagent briefed by the coordinator (DEC-185, DEC-199); started 2026-09-14 15:12, filler while S091 waits for its SPRT night and S222 for its SPSA night
done:       2026-09-14 17:45. **The generation step is proved to follow the tree, and the wiring that runs it on every build is pinned statically by case 6.** tests/test_build_info_freshness.sh drives the real cmake/build_info.cmake as `cmake -DOUTPUT=... -DSOURCE_DIR=... -DARCH=... -DTUNE=... -P`, argument for argument the command line the chesso_build_info target uses, over a throwaway git repository it creates in a temp dir with its own identity, no system or global git config and no hook templates. Script mode has no configure step at all, which is the property: the header follows the tree from the script's own re-run or not at all. **Red first, observed**: a scratch copy under .tuning/coord/ that captures the whole stamp on its first run -- reading it back from a cache file beside the output on every later run, the way a configure-time capture freezes it -- gave five failures across cases 2, 3 and 4, with 1 and 5 green; case 3 goes red there as a consequence of case 2's frozen value rather than on its own property, which is recorded in the test's header so nobody reads it as an independent catch. Case 6 was observed red the same way, against scratch copies of the two CMake files with the word ALL deleted from the custom target and the add_dependencies line deleted from the library: two failures, one per anchor, with cases 1 to 5 green -- which is exactly the point of it, since a stale header is invisible to a test that only runs the script. Two further mutants, a call moved to a configure-time execute_process and a target that keeps ALL but loses -DARCH and the script path, were each caught by their own anchor, and the dependency merely commented out rather than deleted is caught too, because comments are stripped before anything is matched. **The six cases**: a clean tree stamps the short sha `rev-parse` answers at that moment with no suffix, and carries the arch and tune values it was passed, " tune" with its leading space under TUNE=ON and empty under TUNE=OFF; a modified tracked file stamps `<sha>-dirty` and committing it stamps the new short sha with no suffix, both transitions read from `rev-parse` at that moment and never from a value typed into the test, with the commit asserted to have moved HEAD so the case cannot pass vacuously; an untracked file is not dirty, the `git diff --quiet HEAD` convention the script states and the run scripts share; an unchanged tree does not rewrite the header, its mtime set to 2001 surviving the re-run -- set into the past first, or "unchanged" and "rewritten a second ago" are the same second -- and a moved commit does rewrite it and carries the new sha; and a plain directory outside any checkout stamps "unknown" with no suffix, its precondition established rather than assumed by asking git for a sha there first and failing the case if it answers; and the wiring itself, read as text out of the top-level and the src CMake files rather than run, because cases 1 to 5 prove a property of the generation step and not one of them proves that anything invokes it -- the custom target exists, carries ALL and passes OUTPUT, SOURCE_DIR, ARCH and TUNE on the command line that runs the script, no execute_process or configure_file in the top-level file names the script or the generated header, and the engine library still depends on the target. Case 1 also establishes its own precondition now, asking git whether the sandbox tree is clean rather than inferring it from an absent suffix, which is what an earlier dead check against `*-dirty` was reaching for and could never fire. Case 4 requires the mtime it read to be non-empty before it reads it as a date, because an unreadable mtime is arithmetic zero and would have passed a past-mtime guard while proving nothing. **Green and registered**: green against the real script and the real CMake files; registered in tests/CMakeLists.txt beside test_gate_extra_script at LABELS "fast" TIMEOUT 60, taking `CMAKE_COMMAND` rather than whatever cmake is first on PATH and the two CMake files by `CMAKE_SOURCE_DIR`; `bash -n` parses it; 0.16 s under ctest, and the whole fast label 39 of 39 in 118.2 s after a rebuild that re-ran the configure. The sandbox also unsets the git variables that override -C -- GIT_DIR, GIT_WORK_TREE, GIT_INDEX_FILE, GIT_CONFIG_GLOBAL and GIT_CONFIG_COUNT -- which a hook, `git bisect run` or `git rebase --exec` exports: with GIT_DIR and GIT_WORK_TREE pointing at this repository the test died at its own setup before that, and is green after. **Docs**: DEV_MANUAL.md's build-time stamp paragraph gains one sentence naming the test and both halves it covers; the 2026-09-12 plan adversarial review's F02 Status line moves from planned to closed in two halves -- the generation step proved, the wiring pinned -- naming the test and keeping its Previously trail, and nothing else in that report is touched; MANUAL.md already describes the `id name` form and that the sha is stamped at build time (S212) and needs no change. cmake/build_info.cmake itself is unchanged -- no case found it wrong. No `src/` change: `No functional change`.

## Why this exists

The owner's plan review of 2026-09-12 (F02) found S212's accepts naming
configure time for the stamp -- the trap `cmake/build_info.cmake` was written
to avoid -- and asked for a property test that a rebuild without a reconfigure
moves the identity. S212 landed the build-time mechanism (DEC-204 (a)); the
test does not exist. A harness-test gap, not reachable in play: filler behind
S225 (DEC-171).

## Cost

Agent work, an hour; the fast suite once.
