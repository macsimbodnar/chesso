id:         S227
goal:       pixello and the debug GUI leave the branch -- the `tests/pixello` submodule, the `CHESSO_BUILD_GUI` option and `debug_gui` target, `tests/debug_gui.cpp` and everything under `tests/assets/gui/` -- on the owner's instruction (DEC-207)
accepts:    `.gitmodules` has no `tests/pixello` entry and the gitlink is removed with `git rm` (`git submodule deinit -f tests/pixello` first, `.git/modules/tests/pixello` gone); `CMakeLists.txt` has no `CHESSO_BUILD_GUI` option and `tests/CMakeLists.txt` no `debug_gui` target, no `add_subdirectory(pixello)`, no `PIXELLO_ENABLE_TESTS`; `tests/debug_gui.cpp` is deleted; `tests/assets/gui/` is deleted whole, `THIRD_PARTY.md` included, and the `file(COPY ... assets)` line still copies `perft_json` and `test_jsons` for the tests that read them; `git grep -i pixello` over the tree outside `adocs/plan_done/`, `adocs/audit/`, `adocs/decisions.md` and `adocs/status.md` returns nothing; `books/fetch_book.sh`'s sentence about the submodules names `nlohmann/json` and `doctest` only; `DEV_MANUAL.md` and `TOOLCHAIN.md` lose every SDL2 and GUI-build line (`MANUAL.md` checked); `README.md` is not touched and the stamp lists its lines 33-34 and 63-68 for the owner to remove; the fast suite is green in both builds from a fresh configure of `build` (the option's removal changes the cache); no `src/` change, `No functional change`
touches:    .gitmodules, CMakeLists.txt, tests/CMakeLists.txt, tests/debug_gui.cpp, tests/assets/gui/, tests/pixello, books/fetch_book.sh, DEV_MANUAL.md, TOOLCHAIN.md, .vscode/launch.json
excludes:   `README.md`; the `nlohmann/json` and `doctest` submodules; anything under `src/`
decisions:  DEC-207, DEC-201
closes:
blocks:
paused_by:
author:     an Opus 5 subagent briefed by the coordinator (DEC-185, DEC-199); started 2026-09-13 14:05 on the idle machine, the owner's instruction of the same day (DEC-207)
done:       2026-09-13 14:15. **Pixello and the debug GUI are out of the tree.** `git submodule deinit -f tests/pixello` cleared the working directory and unregistered the path, `git rm -f tests/pixello` removed the gitlink and took the `[submodule "tests/pixello"]` entry out of `.gitmodules` in the same call -- the file now names `tests/json` and `tests/doctest` only -- and `.git/modules/tests/pixello` (41M) was deleted. `git rm` then took `tests/debug_gui.cpp` (1186 lines) and all 29 files under `tests/assets/gui/`: the twelve piece renders, both copies of each font family with their `LICENSE.txt`, `FONTLOG.txt` and `UFL.txt`, and `THIRD_PARTY.md` itself, which has nothing left to inventory. Staged deletions total 32 files, 1815 lines. **CMake**: `CMakeLists.txt` loses the `option(CHESSO_BUILD_GUI ...)` line; `tests/CMakeLists.txt` loses the whole `# # Test gui` block -- the `if(CHESSO_BUILD_GUI)`/`else()` with its two `message()` calls, `set(PIXELLO_ENABLE_TESTS OFF)`, `add_subdirectory(pixello)`, the `debug_gui` target with its two include directories and two link lines, and the commented-out `add_test(NAME debug_gui ...)` beneath it (18 lines). `file(COPY ... assets)` at line 1 is unchanged and still needed: `test_chesso`, `test_helpers.hpp`, `test_movegen`, `test_perft` and `test_engine` read `assets/test_jsons/...` and `assets/perft_json/...` by relative path, and `build/tests/assets` now holds exactly those two directories. **`.vscode/launch.json`**: the `debug_gui` launch configuration (8 lines) pointed at `build/tests/debug_gui`; removed as a trivial in-scope discovery and added to `touches:`. **`books/fetch_book.sh`**: the bundling list names two submodules, `nlohmann/json` and `doctest`; the GUI-artwork clause is gone and the paragraph's history now ends with S227 removing the GUI, its graphics submodule and every remaining asset, so no third-party artwork is left in the tree to inventory (DEC-207). DEC-201's blanket -- the repository bundles nothing whose licence is unstated -- still holds and is still stated. The replacement deliberately does not use the word the accepts greps for. **Greps**, excluding `adocs/plan_done`, `adocs/audit`, `adocs/decisions.md`, `adocs/status.md` and this step file: `pixello` and `Cburnett` return nothing at all; `debug_gui` and `BUILD_GUI` survive only in `adocs/plan.md`'s Open-list entry for this step, which is the coordinator's line to move; `SDL2` survives only in `README.md` lines 33-34. **DOCS, checked, no change needed**: `DEV_MANUAL.md`, `TOOLCHAIN.md` and `MANUAL.md` carry no SDL, pixello or GUI-build line -- `git grep -i sdl` outside `adocs/` returns the two `README.md` lines and one false positive inside the `src/openings.bin` blob; TOOLCHAIN's S226 workstation package list (line 53) never carried SDL2; DEV_MANUAL's submodule recipe (line 1837) already named `tests/doctest` and `tests/json` only, and every "gui" hit in it is "profile-guided". **For the owner, `README.md` untouched** (DOCS): line 33 `# SDL2 is required for the test gui application` and line 34's `sudo apt install libsdl2-*` are now false, and lines **63-69** -- `### Run the test gui application` through the closing fence; the accepts says 63-68 and misses the fence at 69 -- describe a target that no longer exists. That section was already wrong: it says `./test_gui` and the binary was `debug_gui`. **Gate, both builds freshly configured**: `rm -rf build` then `cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_COMPILER_LAUNCHER=ccache`, and the same for `build-tune` with `-DCHESSO_TUNE=ON`, because a stale `CHESSO_BUILD_GUI:BOOL=OFF` entry survives a reconfigure and only a fresh cache drops it. Each new cache differs from the saved old one by exactly that one line and nothing else (`.tuning/coord/S227_cache_before.txt`, `S227_tune_cache_before.txt`); `CHESSO_ARCH=native` and every other value reproduced. The chain `cmake --build build -j12 && ctest --test-dir build -L fast && cmake --build build-tune -j12 && ctest --test-dir build-tune -L fast && ./clang-format.sh --check` exits 0: **37/37 fast in `build` (111.78 s), 37/37 in `build-tune` (113.00 s)**, format check clean under `CLANG_FORMAT_MAJOR=22` (`.tuning/coord/S227_gate.log`). `./build/src/chesso bench | tail -1` prints **7111579 nodes**, the parent's total; no `src/` file was touched, so the commit is `No functional change`. **Local state deleted, none of it tracked**: `build-gui/` (12M, `CHESSO_BUILD_GUI=ON`, configured 2026-09-12 for S224's GUI proof) can no longer be configured and was removed, and stale `assets/gui` copies were cleared from seven other build trees. **Left for the owner, local and untracked**: `.git/modules/pixello` (43 M) and the `[submodule "pixello"]` section of `.git/config`, stale local state from the old repo-root layout, were removed by the coordinator at completion; neither is tracked.git/config` are a stale pair from an older layout that kept the submodule at the repository root; this step does not name them and deleting them is the owner's call. `master` and `bitboard` still declare `tests/pixello`, so a checkout of either followed by `git submodule update --init` re-clones it over the network -- that is the price of deleting `.git/modules/tests/pixello`, and it is the accepts's own clause. **One environmental note, unrelated to this step**: `test_clang_format_script` is red on this machine unless `CLANG_FORMAT_MAJOR=22` reaches `ctest`'s environment -- the script defaults to 23 and only 22 and 18 are installed. Observed red once without the export, green with it.

## Why this exists

The owner's instruction of 2026-09-13 (DEC-207). Pixello was an early
debugging aid; nothing in the gate, the tools or the harness builds it, and it
is the reason the branch carried third-party artwork at all (S211 licensed it,
S224 deleted the sounds). Removing it closes the parked question about the
submodule's own test sounds.

## Cost

Agent work, under an hour; one fresh configure and the gate.


## Note 2026-09-13, from the fast check: the json submodule's test tree

`tests/json/.reuse/dep5` declares its own `tests/thirdparty/imapdl/*` as GPL-3.0-only (and Fuzzer as Apache-2.0, amalgamate as BSD-3-Clause); those files are present after `git submodule update --init` and are neither built, included nor shipped here. `books/fetch_book.sh` says so now, and **S229** carries the library as its MIT single header so that no such file enters a checkout.
