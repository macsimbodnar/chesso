id:         S150
goal:       a number stated in a plan or manual document about a search parameter is checked against search_param_info(), and the three values S085 left stale in specs.md and MANUAL.md are corrected
accepts:    a tracked check reads `search_param_info()` and flags a plan or manual document sentence that states a different number for the same parameter, and it is observed red on each of the three known cases before it is trusted; the three are corrected -- `adocs/specs.md:171` aspiration 5 / 50 / 400 to 2 / 21 / 437, `adocs/specs.md:178` quiescence "capped at 8 plies" to 19, `MANUAL.md:250` "from depth 5" to 2; the check's coverage limit is stated rather than implied, since the two specs.md sentences never name the parameter and a name-adjacency scan misses them, so the phrase set it keys on is written down as the maintenance cost; nothing in `src/` changes
touches:    tools/plan_prose_check.py, tests/CMakeLists.txt, adocs/specs.md, MANUAL.md, DEV_MANUAL.md, adocs/plan.md
excludes:   the evaluation constants, which are not in `search_param_info()`; re-checking numbers in `adocs/plan_done/`, which is history and is never rewritten; the citation checker's own two modes, which S138 and S144 own
decisions:  
author:     claude (Opus 5), coordinator, 2026-08-30
closes:     2026-08-21_adversarial-F02
blocks:
paused_by:
done:       2026-08-30. **`tools/plan_prose_check.py --params` is the check, and it is in the fast suite as `test_plan_params`, 0.23 s** -- the second of the tool's four modes to be gated, on the same reasoning S141 used for `--touches` and for the reason `--citations` is not: it compares a number to a number and holds no line numbers, so no source commit can shift it red. The values come from `src/search_params.hpp`'s `X(symbol, name, default, min, max)` list, which is the single source `search_param_info()` is generated from in both builds and which `tests/test_search_params.cpp` holds the two equal through -- so reading the list is reading the function, and a list that parses to nothing **fails** rather than passing vacuously. **Three rules, and the reason there are three is the coverage limit the `accepts` asked to have stated.** `TABLE` reads MANUAL.md's option rows and compares default, min and max -- new coverage, because `test_uci_surface` requires MANUAL.md to *name* every option and never checks what the row says about it. `NEAR` takes a sentence that names the parameter and then gives a number in one of a few tight forms, tight because "`LazyEvalMargin` at 0, 150 and 2000" is a sweep and a looser rule flags it -- measured, that was the one false positive of the first draft. `PHRASE` is for the sentences that never name their parameter, **which is two of F02's three**, so a name-adjacency scan alone would have missed exactly the cases the finding was about; those are keyed on the wording in `PARAM_PHRASES`, and a rule that matches nothing anywhere prints `STALE` rather than passing quietly, which is what keeps the maintenance cost visible instead of silent. Tense is not understood and that turns out to be load-bearing rather than a gap -- see the fourth case below. **Observed red on all three known cases before anything was corrected**, and the third needed care because it was no longer in the tree: `eaad88b` had already fixed the quiescence cap on 2026-08-22, so `git show eaad88b^:adocs/specs.md` and `eaad88b^:MANUAL.md` were checked out and the run over them printed all five lines -- aspiration 5 / 50 / 400 against 2 / 21 / 437, the quiescence cap 8 against 19, and MANUAL's depth 5 against 2. **Non-vacuity was then proved from the other side**: `AspirationMinDepth`'s default was temporarily moved to 4 in the header and all three rule classes fired at once (`PHRASE` in specs.md, `TABLE` at `MANUAL.md:140`, `PHRASE` at `MANUAL.md:266`); the header was restored and `git diff -- src/` is empty, which the `accepts`' "nothing in `src/` changes" requires. **The check found a fourth case the audit missed**, which is the argument for gating it: `adocs/plan.md:251` said "`MaxQsearchDepth` **is** 8, the bound binds", written when S084 and S085 were pending and stale since S085 shipped 19. Trivial and inside the goal line's "a plan or manual document", so it was fixed here and is noted rather than deferred -- and it was fixed to "**was** 8 when this was written ... S085 shipped 19", which the check then leaves alone, because `NEAR` keys on "is", "ships at", "=" and "default" and a past-tense value reads as history. That is why tense-blindness is the right behaviour: the repair keeps what the sentence records instead of deleting it. The two remaining known cases are corrected in place with the parameter named beside the number, so the next reader can check them by hand: specs.md's aspiration sentence now reads 2 / 21 / 437 and says S085 retuned all three from S021's 5 / 50 / 400, and MANUAL.md's line reads depth 2 and points at its own option table. **One phrase rule was retired rather than kept**: `("MaxQsearchDepth", "quiescence is capped at (\\d+) plies")` was F02's third case, was observed red against `eaad88b^`, and `eaad88b` rewrote that sentence to name the parameter -- so `NEAR` covers it now ("`MaxQsearchDepth` plies, 19 as shipped", verified matching and agreeing at 19), and leaving the rule in would have printed `STALE` on every run until somebody deleted it to get green. The retirement and its reason are in the source where the rule was. Completion gate: build clean, `ctest -L fast` **21/21 in 26.72 s** including the new `test_plan_params`, `clang-format.sh --check` exit 0. Docs: `DEV_MANUAL.md` documents the mode, its three rules, its registration and what it cannot cover; `MANUAL.md` and `adocs/specs.md` carry the corrections; `README.md` owner-written and untouched. No engine behaviour moved, so INV-6 does not arise. `2026-08-21_adversarial-F02` moves open to closed in its own report. Left standing and not this step's: `--citations` reports 97 stale citations over 58 pending step files, which is S138 and S144's class and this step's `excludes` names it.

## Why the existing checks cannot catch this

`tools/plan_prose_check.py` has `--prose` and `--citations` and neither compares
a number to code. `tests/test_uci_surface.cpp:88-91` builds its expected option
lines *from* `search_param_info()`, so it restates the code rather than checking
prose against it. `tests/test_search_params.cpp`'s golden list guards the code
against itself. S139, S140, S141 and S144 check accepts fields, retired ids,
touches fields and citation paths -- none checks a value.
