id:         S158
goal:       S146 carries the shipped book's digest and entry count so its origin can be searched for rather than guessed
accepts:    `adocs/plan_todo/S146_book_provenance.md` carries the decoded book's digest and entry count, so its origin can be searched for rather than guessed: 2610256 bytes, 163141 sixteen-byte Polyglot entries, sha256 `47a817350459843da2a20e1d5cba28462d9df30bdb99c93097bd3cb66ce78fb5`, first entry key `00000883b144421f` move `0dae` weight 45; the command that reproduces the digest from `src/openings.book` is recorded with it; the statement that `Use Book` defaults false (`src/chesso.cpp:930`) is recorded too, because it is what makes this a licence exposure and not a measurement contaminant
touches:    adocs/plan_todo/S146_openings_book_provenance.md  <!-- named S146_book_provenance.md in the accepts; that file does not exist -->
excludes:   establishing the book's actual origin or licence, replacing it, and any decision about it, all of which are S146's and the owner's; the match books under `books/`, which `books/fetch_book.sh` already pins
decisions:  DEC-016
author:     claude (Opus 5), coordinator, 2026-08-30
closes:     2026-08-21_adversarial-F10
blocks:
paused_by:
done:       2026-08-30. Every figure the `accepts` names was **recomputed from `src/openings.book` as tracked, not copied out of the audit**, and all five reproduce exactly: decoded **2610256 bytes**, **163141** sixteen-byte Polyglot entries with no remainder, sha256 `47a817350459843da2a20e1d5cba28462d9df30bdb99c93097bd3cb66ce78fb5`, first entry key `00000883b144421f` move `0dae` weight 45 (learn 0). Written into `S146_openings_book_provenance.md` under `## Identifiers, so the origin can be searched for rather than guessed`, with two reproducer commands rather than one -- a `sed | tr | xxd -r -p | shasum -a 256` pipeline, because this machine has no `sha256sum` (`.moltke.local.md`), and a `python3 -c` form that needs neither and prints the byte and entry counts beside the digest. Both were run and both agree. **Three things the accepts did not ask for and that make the record usable.** The entry count is corroborated at runtime and not left as arithmetic: `load_book_embedded` (`src/openings.cpp:358`) divides by `sizeof(polyglot_entry_t)` and the debug build prints "Opening book loaded correctly! 163141 entries." -- the shipping build cannot be asked, `src/log.hpp:29` compiling `LOG_I` to `if (false) std::clog` under `NDEBUG`, which is worth knowing before someone tries. The source file's own digest is recorded beside the decoded one (`6fd66d8d86cce8e0c639f047d0d6d9cd42a06ca9ba3f91d01601c6ad7f84e026`, 5220541 bytes) so the tracked artifact and the thing a published book would be compared against are not confused. And the blob's introducing commit is named: `349f8cf`, 2025-05-12, "Add openings book", on `bitboard`, whose message is one line and says nothing about origin -- which is the negative result S146 needs, since it closes the last place inside the repository where the provenance could have been hiding. **One statement in the `accepts` was wrong and is corrected rather than transcribed**: `Use Book` defaults false at `src/chesso.cpp:957`, not `:930` -- the audit's line has drifted 27 lines -- and the advertised default is traced to the state that backs it, `opening_book_enabled` initialised `false` at `src/chesso.cpp:31`, moved only by the `setoption` handler at `:1044-1052`, and required by `search_random_move_in_book` at `:606`. That is what makes this a licence exposure and not a measurement contaminant: the book is linked into every shipped binary but no SPRT taken so far has played a move from it, so replacing it invalidates no verdict. The `touches` field names `adocs/plan_todo/S146_book_provenance.md`, which does not exist; the file is `S146_openings_book_provenance.md` and the field is annotated rather than silently corrected. Scope held: nothing about the book's actual origin, licence or replacement was decided, and the `polyglot_randoms[781]` table is left where S146's 2026-08-22 amendment already put it -- both are the owner's call. Completion gate: build clean, `ctest -L fast` **20/20 in 27.00 s**, `clang-format.sh --check` exit 0. No code changed, so INV-6 does not apply. Docs: `README.md` owner-written and untouched; `MANUAL.md` unchanged -- documenting what the shipped book is belongs to S146's `accepts`, not here, and no UCI surface moved; `DEV_MANUAL.md` unchanged -- the two reproducer commands are evidence for one step's finding, not a project workflow, and live in the step file; `specs.md` unchanged, no behaviour moved. `2026-08-21_adversarial-F10` moves open to closed in its own report.

## Why this is a step and not just a note

S146 cannot close without an identifier to search on, and the digest costs
nothing to compute. Splitting it out keeps S146's own decision -- establish,
replace, or record -- untouched by an agent.
