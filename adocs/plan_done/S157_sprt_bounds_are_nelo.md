id:         S157
goal:       every recorded conclusion that states an SPRT bound in plain Elo says nElo instead, and DEV_MANUAL.md says which scale the bounds are in
accepts:    `DEV_MANUAL.md` and `fastchess.sh` state that the bounds are normalized Elo, beside the CPW table they are taken from, so the scale is connected to the numbers rather than left to be re-derived; every recorded conclusion that states a bound in plain Elo is restated -- the eight sentences of the form "not a regression of 5 Elo or more" in `adocs/specs.md` and the pending-side documents -- with the logistic equivalent given from the run's own printed `Elo` and `nElo` pair rather than from a general constant, since the ratio is a property of the draw rate; `model=normalized` is **not** changed and the reason is stated, because the CPW table's own Stockfish rows are fishtest's nElo bounds, so the current setting matches the cited source
touches:    DEV_MANUAL.md, fastchess.sh, adocs/specs.md, adocs/data/README.md, adocs/status.md
excludes:   changing the SPRT model or any bound pair, which would alter every verdict's cost and is not what this is about; rewriting `adocs/plan_done/`, which is history -- the restatement lands in `specs.md`'s current-state wording and in `DEV_MANUAL.md`; re-running any earlier verdict
decisions:  DEC-063
author:     claude (Opus 5), coordinator, 2026-08-30
closes:     2026-08-21_adversarial-F09
blocks:
paused_by:
done:       2026-08-30. **The scale is now stated where the bounds are set and where they are read.** `fastchess.sh`'s WHICH BOUNDS block and `DEV_MANUAL.md`'s "Which bounds" section both say the pair is normalized Elo, quoting the binary rather than asserting it -- `fastchess --help` on this machine prints "normalized - Uses nElo (default)" -- and both stand beside the CPW citation they qualify, which is what the `accepts` asked for. `model=normalized` is **unchanged** and both places now carry the reason: the CPW table states no scale of its own, but its rows are Stockfish STC `{0,2}` and LTC `{0.5,2.5}`, which are fishtest's bounds, and fishtest expresses bounds in normalized Elo -- so `model=normalized` is what makes `{0,5}` mean what the source it was taken from means, and switching to `logistic` to make the numbers read as Elo would re-price every verdict this project has taken. **The restatement covers seven sentences, not the audit's eight, and every logistic figure comes from its own run's printed pair rather than from a constant** -- the ratio being a property of the draw rate, which the spread demonstrates: 5 nElo is **3.54** logistic Elo at S165's 44.75 % draws (`nElo 1.34` / `Elo 0.95`), **3.64** at S108's 42.20 % (`3.13` / `2.28`), **3.87** at S076's (`34.44` / `26.68`), **3.92** at S107's (`16.18` / `12.67`), **3.94** at S093's (`13.63` / `10.73`) and **3.99** at S021's (`44.04` / `35.12`). Point-estimate and interval-width ratios were computed separately as a cross-check and agree to under 1 % in every case, which is why a single ratio per run is quoted. Five of the seven are in `adocs/specs.md` (the audit's grep found four files plus specs and counted the specs occurrences as two; there are five, on lines 226 and 233), one is `DEV_MANUAL.md`'s S076 write-up and one is `status.md`'s S108 line in the parked handover -- both current-side documents the audit's grep pattern did not reach because they phrase the claim differently. `adocs/plan_done/` is untouched as the `excludes` requires. **One thing the audit got backwards and this step corrects rather than repeats: the two bound sides are wrong in opposite directions.** A non-regression verdict rejects "at or below `elo0`", so `--nonreg` actually excludes a regression of about 3.5 to 4.0 *logistic* Elo, which implies the recorded "5 Elo or more is excluded" and then some -- those sentences are conservative and true as written. The gainer side is the dangerous half: accepting H1 at `elo1=5` establishes a gain of 5 nElo, about 3.9 logistic Elo, so "gains 5 Elo or more" claims about 1.1 logistic Elo the run did not demonstrate. That asymmetry is now in `DEV_MANUAL.md` and in `adocs/data/README.md`, and specs.md's one gainer conclusion (S093's) is restated with its 3.94. **Ten pre-registration scripts under `adocs/data/` carry the same plain-Elo wording and were deliberately not edited**: they are the evidence of runs already taken and that directory's own README says a file there "is added, never edited", so falsifying a pre-registration to fix a scale note would be the worse error. Instead an appended section names all ten by file and line, states the scale once, and points at `DEV_MANUAL.md` for where the next pre-registration should be written from -- an append, which is what that README's rule permits. Completion gate: build clean, `ctest -L fast` **20/20 in 27.18 s**, `clang-format.sh --check` exit 0, `bash -n fastchess.sh` clean (the change there is comment-only; no bound, model or flag moved, so no verdict's cost changes and INV-6 does not arise -- no code was touched at all). Docs: `README.md` owner-written and untouched; `MANUAL.md` unchanged and checked -- it carries no SPRT, bound or model statement at all (`grep -n "SPRT\|elo0" MANUAL.md` is empty), so there is nothing there to restate; `DEV_MANUAL.md` and `specs.md` are the change. `2026-08-21_adversarial-F09` moves open to closed in its own report.

## The arithmetic

`fastchess.sh:226` passes `model=normalized`; `fastchess --help` says that model
"Uses nElo". S085 printed `Elo 21.02` and `nElo 26.81`, a ratio of 1.2755, so at
that draw rate a bound of 5 nElo is 3.92 logistic Elo. `DEV_MANUAL.md:1274`
already records that verdicts *report* nElo; what is missing is that the bounds
are in it too.
