id:         S223
goal:       the load boundary requires exactly one king a side and refuses a position whose side not to move is in check, so no accepted `position` line can leave a board without a king or let a supplied move capture one
accepts:    `load_FEN()` in `src/bitboard.cpp` refuses two further classes after S208's two, each with its own reason through the `reason` out-parameter S208 added: a placement with other than one king of either colour, and a placement in which the side **not** to move is in check -- the engine's own attack test from that king's square, which also makes two adjacent kings a refusal -- and `position fen` reports each in the S176 shape with the board, the history and every move applied since the last `position` left as they were; **red first** in `tests/test_audit_fen_semantics.cpp` on the audit's `7k/8/8/8/8/8/8/K6R w - - 0 1` (observed before the fix: `moves h1h8` accepted and the `fen` command answering `7R/8/8/8/8/8/8/K7 b - - 0 1`, the black king gone; after: one `info string refused` line and the prior FEN echoed back), on the kingless `8/3p4/8/8/8/8/3P4/8 w - - 0 1` and on a two-white-king board, with controls that the start position and a position whose side **to** move is in check still load; the refusal agrees with python-chess 1.11.2 `Board.status()` -- `STATUS_NO_WHITE_KING`, `STATUS_NO_BLACK_KING`, `STATUS_TOO_MANY_KINGS`, `STATUS_OPPOSITE_CHECK` -- over every FEN literal in `tests/`, `src/` and `tools/` (3204 on 2026-09-12 by `adocs/data/S223_fen_census.py`, re-run at completion), 0 disagreements on the positions S208's classes do not refuse first; the seven positions the census names below are **re-picked to legal ones with the oracle's word and none deleted**, each re-pick keeping the property its case asserts and any golden derived from it re-derived by its script (DEC-142); the S208 control "the kingless debug positions still load" and the two "survivable" cases in `tests/test_search.cpp` are **re-stated as refusals at load** -- a re-statement and not a relaxation, their premise being a board the boundary no longer admits, recorded in DEC-197; `make_move()` gains a Debug assertion that each side still has exactly one king after the move, exercised by DEC-141's four-round Debug self-play with `Assertion` grepped, so "no accepted input can remove a king" rests on the load bound, legal-only generation (INV-1) and that assertion rather than on a universal test; the no-king branches in `is_check()`, `generate_moves_impl()` and `king_shelter_features()` become Debug assertions, with `bench_movegen` read before and after and its resolution stated; the `empty` shortcut leaves `position`, `EMPTY_POS` leaves `src/data_structures.hpp`, its row leaves `MANUAL.md`, and `test_uci_surface`'s token list is refreshed **after** `MANUAL.md` and `adocs/specs.md` describe the two refusals and the removed shortcut (SURFACE); `adocs/specs.md`'s position-input row names the two classes beside S208's and stops saying legality at large is unchecked in those two respects; INV-6 discharged on identical `tools/search_bench.py` node counts and best moves at two depths and an identical `bench` total, the commit footer `No functional change`; the fast suite green in both builds and `tools/gate_extra.sh` run, because `make_move` and the generator are touched
touches:    src/bitboard.cpp, src/chesso.cpp, src/data_structures.hpp, src/evaluation.cpp, tests/test_audit_fen_semantics.cpp, tests/test_search.cpp, tests/test_chesso.cpp, tests/test_engine.cpp, tests/test_movegen.cpp, tests/test_evaluation.cpp, tests/test_eval_positions.hpp, tests/test_uci_surface.cpp (golden), MANUAL.md, DEV_MANUAL.md, adocs/specs.md, adocs/data/
excludes:   any change to move generation or make/unmake beyond the assertion; pawn counts below the 16-a-side bound, promoted-piece plausibility, checker-count plausibility (python-chess's `TOO_MANY_CHECKERS` and `IMPOSSIBLE_CHECK`) and reachability at large -- the engine plays those correctly and no consumer indexes on them; a permissive internal loader beside the public one, which DEC-177 rejected in as many words; a run-time bound inside the hot loop
decisions:  DEC-171, DEC-177, DEC-197
closes:     2026-09-12_adversarial-F01
blocks:
paused_by:
author:     an Opus 5 subagent briefed by the coordinator (DEC-185, DEC-199); started 2026-09-13 17:30 on the idle machine
done:       2026-09-13. **No accepted `position` line can leave a board without a king, and none can hand a supplied move an enemy king to capture.** `load_FEN()` refuses two further classes after S208's two, each naming its own reason through the `reason` out-parameter S208 added: **`other than one king of each colour (<n> white, <n> black)`**, counted in S208's existing single pass over `squares[]` and tested after both of its refusals so the F09 and back-rank fixtures keep hearing S208's reasons; and **`the side not to move is in check`**, the engine's own `is_attacked()` run from that king's square, which sits below the occupancy fill because the slider rays read `occupancies[BOTH]` and above that point it is still zero. Two adjacent kings are refused by that same test and needed no rule of their own. `position fen` reports both in S176's shape with the whole game restored, which `load_position()` and S210's transactional `command_position()` already did. **Every red observed on `ab5cd8e` before a line of the fix was written.** By hand on the Release binary: `printf 'position fen 7k/8/8/8/8/8/8/K6R w - - 0 1 moves h1h8\nfen\nquit\n' | ./build/src/chesso` -> **`7R/8/8/8/8/8/8/K7 b - - 0 1`**, the black king captured by a move the engine accepted, nothing said about it; `8/3p4/8/8/8/8/3P4/8 w - - 0 1` and `4K3/8/8/8/8/8/8/K6k w - - 0 1` both loaded and were echoed back by `fen`. As ctest, nine new cases in `tests/test_audit_fen_semantics.cpp`: **29 cases, 23 passed, 6 failed; 95 assertions, 20 failed** on the unfixed tree, and the three **controls passed before and after** -- "S208's two reasons still fire first", "the start position still loads" (with the king count asserted at 1 and 1 so it cannot pass on a kingless board) and "a position whose side to move is in check still loads" (`4k3/8/8/8/8/8/8/R3K2r w Q - 0 1`, `is_check()` asserted true so a rule that refused every check would fail here). The ninth drives the finding end to end through `position fen` and asserts the board is still `KIWIPETE_POS` afterwards. Green after: **29 of 29, 100 assertions**. **The `make_move()` assertion is not vacuous, and that was measured rather than argued**: a throwaway worktree carrying this step's `src/` with only the two new refusals neutered (`if (false && ...)`), built Debug, answers the F01 line with `Assertion 'count_bits(board->bitboards[B_KING]) == 1' failed` at `make_move_impl`. So "no accepted input can remove a king" rests on the load bound, INV-1's legal-only generation and that assertion, and the middle term is the only one not directly observed. **Agreement with the oracle, scripted.** `adocs/data/S223_fen_census.py` gained a second pass that drives `build/src/chesso` over UCI -- one `position fen` and one `fen` per literal, reading the `info string refused` line -- and compares *whether the engine refuses* against *whether python-chess 1.11.2 reports one of the four king flags*, exiting non-zero on a disagreement. At the completing tree: **3214 unique FENs, 3201 pass the king gate, 13 flagged, 3199 compared, 10 refused earlier by S208's classes, 3 by the parser or a field bound, and 0 disagreements** (`adocs/data/S223_fen_census_after.txt`; the 2026-09-12 run stays as `S223_fen_census.txt`). The comparison is narrow on purpose: python-chess also reports `BAD_CASTLING_RIGHTS`, `INVALID_EP_SQUARE` and `TOO_MANY_CHECKERS`, none of which this engine refuses and none of which it should. All 13 still flagged are a refusal case asserting the refusal, a fixture S208 refuses first, or a FEN that appears only in a comment; none is a position any test loads expecting success. A companion scan of the **37 bare placement literals** completed with ` w - - 0 1` -- invisible to the census regex, which needs four fields -- found **0** that the new bound would refuse. **The re-picks: eight FEN literals over fourteen sites, none deleted, every verdict python-chess 1.11.2's.** The step file's table says "seven FENs over nine sites" and undercounts both; the sites are as the census listed them. (1) `4k3/8/8/8/8/8/8/R6R w KQ - 0 1` (`NO_WHITE_KING`, one site) -- the premise is gone, so "a right with no king anywhere is cleared" states the refusal instead, and the clearing property it held moves one case up to "castling rights with the king displaced license no castle", which now also asserts `castling == 0` on `3k4/8/8/8/8/8/8/3K3R w K - 0 1` (python-chess: `Status.BAD_CASTLING_RIGHTS` only, none of the four king flags; the stamp first said `VALID`, corrected by the coordinator after the fast check): the same rule with a king on the board. (2) `4k3/8/4R3/3Pp3/8/8/8/4K3 w - e6 0 1` (`INVALID_EP_SQUARE,OPPOSITE_CHECK`, one site) -> **`4k3/8/4N3/3Pp3/8/8/8/4K3 w - e6 0 1`**, the e6 occupant a knight, which attacks d8 and f8 rather than the black king on e8; the oracle then reports `INVALID_EP_SQUARE` and nothing else, which is the case's own subject, and the target square is still occupied so the same branch clears the field. (3) `3k4/8/7p/Q1p3pP/1pPpPpP1/1P1PpP2/N7/2K5 w - - 0 1` and (4) `3k4/8/7p/2p3pP/1pPpPpPQ/1P1PpP2/N7/2K4r b - - 0 1` (`OPPOSITE_CHECK`, three sites each) -- in "Test is in check" the two `false` rows take the checking piece off instead of flipping the turn: **`3k4/8/7p/2p3pP/1pPpPpP1/1P1PpP2/N7/2K5 w - - 0 1`** and **`3k4/8/7p/2p3pP/1pPpPpPQ/1P1PpP2/N7/2K5 b - - 0 1`**, both `Status.VALID` with `Board.is_check()` **False**, against the two kept `true` rows at `Status.VALID` and `is_check()` **True**; in "Test is_attacking_king" the same two boards take the **other** side to move (`...Q1p3pP... b` and `...2K4r w`, both `Status.VALID`), because `is_capturing_king()` reads the move and the two king bitboards and never `active_color` -- with the turn as it was, the side to move was the one attacking the enemy king, which is the refused class itself. (5) `4r3/8/8/8/4N3/8/8/4K3 w - - 0 1` (`NO_BLACK_KING`, three sites: `tests/test_movegen.cpp` "a pinned piece cannot leave the ray" and two `position fen` lines in `tests/test_engine.cpp`) -> **`k3r3/8/8/8/4N3/8/8/4K3 w - - 0 1`**, `Status.VALID`: a black king on a8, off the pinning ray, out of every white attack and adjacent to neither king, so the pin, the white king's move list and `check_move_legality`'s three answers are untouched -- and `position_is_reachable()` in the movegen case stops passing vacuously, having answered true on a board with no black king to test. (6) `8/PPPPPPPP/8/2k5/2K5/8/pppppppp/8 w - - 0 1` (`OPPOSITE_CHECK`, adjacent kings, one site in `tests/test_eval_positions.hpp`) -> **`8/PPPPPPPP/8/1k6/6K1/8/pppppppp/8 w - - 0 1`**, `Status.VALID`: same two pawn walls, kings on b5 and g4, each still with three own-pawn files under it, so every pawn, passed-pawn and king-shelter count the row contributes is unchanged and only the two kings' piece-square entries move. (7) `4k3/8/8/8/8/8/8/8 w - - 0 1` and (8) `8/8/8/8/8/8/8/4K3 w - - 0 1` (`NO_WHITE_KING`, `NO_BLACK_KING`, one site each) -> the one board **`4k3/8/8/8/8/8/8/4K3 w - - 0 1`**, `Status.VALID`, which asserts the same thing about both kings at once: a board whose only pieces are kings has `material == 0` only if neither is priced. Its case is renamed "a king is not worth anything"; the two non-vacuity loads beside it are untouched. **No golden moved.** `test_eval_model.cpp`'s `truncation_positions` are four other FENs and none is re-picked; `test_tuner_gradient.cpp`'s `psqt_nonzero > 120` is a floor its own comment says is not a pinned number; no expected node figure in `tests/` is derived from a re-picked position. Checked by `grep -rn 'GOLDEN (DEC-142)' tests/` and by the suite. **Three re-statements, recorded as DEC-197 requires and none of them a relaxation.** The S208 control "the kingless debug positions still load" becomes "the debug positions still load" over the nine named constants that remain -- `EMPTY_POS` and `8/3p4/...` are gone from it because the boundary no longer admits them, and the half it was actually holding, that S208's bound did not eat the positions it was measured on, is asserted over more constants than before. The two "survivable" cases in `tests/test_search.cpp` become "a position with a capturable king never reaches the search" and "a board with no kings never reaches the search": S067's survival was real and is why F01 is a refusal and not a corruption, but with the premise removed "the search survives it" is a statement about nothing, and the boundary is the stronger claim. **The three branches are Debug assertions**: `is_check()`'s `if (king == BB_0) return false`, `generate_moves_impl()`'s `INVALID_INDEX` king square and its whole `if (king_bb)` block (de-indented, same order, same calls), and `king_shelter_features()`'s early return in `src/evaluation.cpp`. `make_move_impl()` gains `count_bits(W_KING) == 1` and `count_bits(B_KING) == 1` before its existing en-prise assertion, whose `== BB_0 ||` guard is gone with them. **A fourth branch of the same kind was found and left**: `king_zone()` in `src/evaluation.cpp` returns 0 on an empty king bitboard. The accepts names three; this one is now unreachable for the same reason and is a one-line follow-up, not a defect. **`bench_movegen`, before and after, three interleaved runs each on an idle machine** (`/proc/loadavg` 1.3-2.0, nothing above 3.5 % CPU): node counts **identical and verified against the published perft values** -- 41812668 for the perft total and 12000000 for the generation loops. Timings are not a claim: perft total 1108.2 / 1114.4 / 1100.2 ms before against 1049.2 / 1037.5 / 1093.3 after, a 5 % median gap inside a 5.4 % spread within the candidate's own three runs; `generate_moves` 806.5 / 813.3 / 813.1 against 810.1 / 809.3 / 802.1; `generate_captures` 334.1 / 332.3 / 332.6 against 327.4 / 329.9 / 330.2. The runs reported resolutions of **0.1 % to 3.7 %**, so nothing here is above its own noise floor and the step claims no speed-up. **INV-6 discharged, `No functional change`.** `bench` **7105111** before and after, and `tools/search_bench.py` node-identical at two depths: depth 9 **47635 / 213916 / 26130** with `c3d5` / `e2a6` / `d7c8q`, depth 12 **141455 / 1038779 / 175684** with `c3d5` / `d5e6` / `d7c8q`. The parent binary was kept aside before the first edit and measured, not quoted. **SURFACE, in order.** `MANUAL.md` first: the two refusal shapes are in the `position` block, the "only two placement rules" paragraph becomes four with the reason for each and the F01 reproduction quoted, the "legality at large" paragraph now says what is still unchecked (pawn counts, promoted-piece plausibility, checker counts, reachability) instead of naming one king a side, and the `empty` row leaves the shortcut table with a line saying when and why. `adocs/specs.md`'s position-input row is **proposed, not written** -- an agent does not touch the shared documents -- in `.tuning/coord/S223/specs_proposal.md`, one sentence replaced in one row. Then `tests/test_uci_surface.cpp`: the two templates join `expected_refusal_templates`, which is what holds `MANUAL.md` to documenting them, and `empty` leaves `expected_position_tokens`. Because DEC-028 leaves that list hand-maintained, a token removed from it is not otherwise checked, so the surface case now parks the engine on `fine70`, sends `position empty` and asserts the board did **not** move -- which fails if the shortcut ever comes back. `EMPTY_POS` leaves `src/data_structures.hpp` with a note saying where it went. **Gate: `ctest -L fast` 38/38 in `build` and 38/38 in `build-tune`**, format clean with `CLANG_FORMAT_MAJOR=22` exported (DEC-146; without it `test_clang_format_script` fails on this machine looking for clang-format 23, which is environment and not this change). **DEC-141's second tier, both clauses that bind.** Debug self-play, four rounds at 4+0.04 on `build-debug`, the DEV_MANUAL form with `level=trace engine=true`: **8 games in 16 s, 0 `Assertion` in both the trace log and the tee'd stdout, 0 `disconnect`**, logs in `.tuning/coord/S223/`. `tools/gate_extra.sh` on this tree: **`GATE-EXTRA-DONE 5 stages 1059 s`** -- prose 0 s, citations 0 s, Debug 304 s, sanitizer 696 s, deep perft 59 s, every stage ok (`.tuning/gate_extra_2026-09-13_S223.log`, stage logs in `/tmp/chesso_gate_extra_20260913_180626`). The mutant clause does not bind -- this is a load-boundary refusal and not a pruning, reduction or extension rule -- and two mutants for the new rules are a follow-up rather than an omission: `tools/mutation_check.py` anchors into a linked worktree at a **commit**, and the anchors do not exist until this change is committed. **Beyond the accepts, noted rather than hidden**: `adocs/data/S223_fen_census.py` gained the engine pass and `adocs/data/README.md` two rows, neither named in `touches:` beyond `adocs/data/`; `tests/test_eval_positions.hpp`, `tests/test_engine.cpp` and `tests/test_movegen.cpp` are in `touches:` and were edited only at the re-picked sites. `DEV_MANUAL.md` was checked and needs nothing: no command, build, label count or quoted number in it moves (DOCS). `README.md` untouched, human-owned. Closes `2026-09-12_adversarial-F01`.

## Why this exists, and where it sits

`2026-09-12_adversarial-F01`, the owner's Codex audit against `98af071`.
`position fen 7k/8/8/8/8/8/8/K6R w - - 0 1 moves h1h8` is accepted and the
`fen` command then answers `7R/8/8/8/8/8/8/K7 b - - 0 1`: the black king has
been captured. Reproduced by the coordinator the same day on the Release
binary. The position is one no game reaches -- python-chess reports
`STATUS_OPPOSITE_CHECK` -- and S161 and S208 both left it admitted on purpose:
"legality at large is deliberately unchecked; one king a side is not
required, because `EMPTY_POS` and two survivable cases are kingless on
purpose" (`src/bitboard.cpp`, section 6a; S208's `excludes:`).

**Why it is not the fix-first bug the report calls it.** DEC-171 scopes BUGS
by reach: a defect is fixed before the next strength step when it is
reachable in ordinary play, on the UCI surface as GUIs and harnesses drive
it, or able to move a reported score. This one needs an illegal position;
no GUI, book, harness, corpus tool or `datagen` sends one; no memory is
corrupted -- the bitboards and the square array agree after the capture,
which is exactly what S067's two "survivable" cases pin, unlike F09's stack
overrun and F10's out-of-bounds write; and no legal position's score, move
or line changes. So it is filler behind S109, beside S210, closed by the
block boundary it sits in and named in the pre-registration of every run
taken while it is open. DEC-197.

**Why it is still worth doing.** It is the class S208 closed, one boundary
further, and it is free where S208's was free: a check at load time and
nothing in the hot loop. Three branches exist today only for the state it
removes -- `is_check()` returns false on an empty king bitboard,
`generate_moves_impl()` carries an `INVALID_INDEX` king square, and
`king_shelter_features()` returns early -- and each is a hot-path branch that
becomes an assertion. Two planned steps index by the king's square with no
guard of their own: S133's king-relative tables and S029's network features.
Closing the boundary now means neither has to learn about kingless boards.
And the prime directive's board-state clause reads the way the report reads
it once the position is refused rather than survived.

## What the census found, 2026-09-12

`adocs/data/S223_fen_census.py` extracts every FEN literal from `tests/`
(excluding the vendored `tests/json/`), `src/` and `tools/` and asks
python-chess 1.11.2 for `Board.status()`: 3204 unique FENs, 3188 pass the
king and opposite-check gate, 16 do not (`adocs/data/S223_fen_census.txt`).
The sixteen, by what this step does with them:

| class | positions | what happens |
|---|---|---|
| already refused by S208 before any king check | the F09 27-queen board; `pppppppp/8/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1` in `tests/test_movegen.cpp` (a pawn on rank 8) | nothing; the piece-count and back-rank reasons must keep firing first, their tests pin the reason text |
| syntax fixtures the parser already refuses | the nine-piece rank and the seven-piece rank in `tests/test_movegen.cpp` | nothing |
| become refusal cases | `EMPTY_POS`; `8/3p4/8/8/8/8/3P4/8 w - - 0 1`; the audit's `7k/8/8/8/8/8/8/K6R w - - 0 1` | the S208 control and the two survivable cases in `tests/test_search.cpp` re-stated as refusals at load |
| a comment, never loaded | `2k5/8/8/8/8/8/1q6/K1R5 w - - 0 1` in `tests/test_search.cpp`, the S067 anecdote | nothing |
| re-picked, seven FENs over nine sites | `4k3/8/8/8/8/8/8/R6R w KQ - 0 1` ("a right with no king anywhere is cleared"): the premise is gone, the case re-states as a refusal and the off-square-king case beside it keeps the clearing property; `4k3/8/4R3/3Pp3/8/8/8/4K3 w - e6 0 1` ("an occupied ep target square goes"): the rook on e6 checks the king not to move, so the occupant is re-picked to a piece that does not; `3k4/8/7p/Q1p3pP/1pPpPpP1/1P1PpP2/N7/2K5 w - - 0 1` and `3k4/8/7p/2p3pP/1pPpPpPQ/1P1PpP2/N7/2K4r b - - 0 1` in `tests/test_chesso.cpp` ("Test is in check", "Test is_attacking_king"): both have the side not to move in check, re-picked with `is_check()` and `gives_check()` from the oracle as the expected values; `4r3/8/8/8/4N3/8/8/4K3 w - - 0 1` in `tests/test_engine.cpp` (two sites) and `tests/test_movegen.cpp` (the pinned knight): no black king, one added off the ray and out of every white attack, one FEN for all three sites; `8/PPPPPPPP/8/2k5/2K5/8/pppppppp/8 w - - 0 1` in `tests/test_eval_positions.hpp` (the promotion-phase anchor): adjacent kings, re-picked with the kings apart and the phase sum still above a full board, which is what it is there for; `4k3/8/8/8/8/8/8/8 w - - 0 1` and `8/8/8/8/8/8/8/4K3 w - - 0 1` in `tests/test_evaluation.cpp` (a king is worth nothing to the material accumulator): lone kings, re-picked as the two-king board, which asserts the same thing about both kings at once | each with the oracle's `Status.VALID` quoted at the site |

## Hazards

- **Order.** S208's two refusals come first and keep their reasons; two of
  their tests pin the text ("27 white"). The king count comes before the
  check test: an attack test from a lone king's square on an empty bitboard
  reads square 64, the very read the survivable cases were written for.
- **The eval anchor feeds two test binaries.** `test_eval_positions.hpp` is
  read by `test_eval_model` and `test_tuner_gradient`; a golden that any of
  them derives from the list is re-derived by its script, never re-read
  (DEC-142).
- **`test_chesso.cpp`'s expected booleans come from the oracle**, not from
  the old table. `python-chess` `Board.is_check()` and `gives_check(move)`.
- **No `Bench:` line is expected.** Every bench position is legal since
  DEC-177. If the total moves, stop and find out why before committing.
- **`gate_extra.sh` binds.** `make_move` gains an assertion and the generator
  loses a branch; DEC-141's Debug self-play and the sanitizer build run
  before completion.

## Cost

Agent work, half a day to a day with the census re-run and the re-picks; no
match. Filler behind S109 under DEC-171; `2026-09-12_adversarial-F01` is
named in every pre-registration while this step is open.
