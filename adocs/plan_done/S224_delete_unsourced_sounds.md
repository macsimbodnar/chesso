id:         S224
goal:       the seven sound files under `tests/assets/gui/sound/` whose origin nobody can state are deleted with their loader, so every file the repository bundles carries a licence `THIRD_PARTY.md` can quote (DEC-201)
accepts:    `tests/assets/gui/sound/` is gone (`click.wav`, `tick_1.wav` to `tick_5.wav`, `anime-wow-sound-effect.mp3`); `tests/debug_gui.cpp` no longer declares `sound_t`, `sound_fx`, `load_sound` or `play_sound`, and no call site remains; the GUI builds with `-DCHESSO_BUILD_GUI=ON` and runs against the display for five seconds without a missing-asset message; `tests/assets/gui/THIRD_PARTY.md` loses its "remaining gap" section and states that every file in the directory is listed with its licence; `books/fetch_book.sh`'s sentence about what the repository bundles no longer names the sounds as an exception; `grep -rn 'sound' tests/assets/gui/THIRD_PARTY.md books/fetch_book.sh tests/debug_gui.cpp` returns nothing; the fast suite is green in both builds and the commit touches no `src/`
touches:    tests/assets/gui/sound/, tests/assets/gui/THIRD_PARTY.md, tests/debug_gui.cpp, books/fetch_book.sh
excludes:   the fonts and the pieces, already licensed by S211; any other GUI change; anything under `src/`
decisions:  DEC-201
closes:
blocks:
paused_by:
author:     an Opus 5 subagent briefed by the coordinator (DEC-185, DEC-199); started 2026-09-12 23:15 as filler before S151's match
done:       2026-09-12 23:25. **The seven files are gone and nothing plays them.** `git rm` deleted `tests/assets/gui/sound/click.wav`, `tick_1.wav` .. `tick_5.wav` and `anime-wow-sound-effect.mp3`; the directory went with the last file. `tests/debug_gui.cpp` lost 28 lines and nothing else: the two effect-name constants at the top, the `std::map<std::string, sound_t>` member, the seven `load_sound` lines in `on_init`, the commented-out one at the end of the constructor, and all eight `play_sound` call sites with the three comments that introduced them -- the flip, clipboard and reset buttons, the animation start and end, and picking a piece up and putting it down. `sound_t`, `load_sound` and `play_sound` are pixello's API, never declared here, so there is nothing of them left to declare. The diff is deletions only; `clang-format-22` reformatted no surviving line. **GUI build and run**: `cmake -S . -B build-gui -DCMAKE_BUILD_TYPE=Release -DCHESSO_BUILD_GUI=ON`, `cmake --build build-gui --target debug_gui -j12` -- 0 warnings, 0 errors -- and `timeout 5 ./build-gui/tests/debug_gui` from the binary directory: **exit 124, no output at all**, no missing asset and no SDL_mixer message. Run a second time with a window poll, because a silent five seconds proves less than a window: `xwininfo -root -tree` found `0xe0000a "Chesso": ("debug_gui" "debug_gui") 1101x600` mapped on the session's X display, and the process again exited only by its timeout. The configure step copies `assets/` into the build tree, and the copy under `build-gui/` has no audio directory -- the GUI runs with the files already absent. **Accepts grep**: `grep -rn 'sound' tests/assets/gui/THIRD_PARTY.md books/fetch_book.sh tests/debug_gui.cpp` returns nothing, exit 1. **Prose**: `THIRD_PARTY.md` loses its gap section and opens with the claim instead -- every file in the directory listed with its licence, no exception, 29 files as `find tests/assets/gui -type f | wc -l` counts, which is twelve piece renders, two font families present twice each, their licence and FONTLOG files, and itself -- and its deleted-files section now reads "Deleted by S211 and S224" with a paragraph on the seven, pointing at DEC-201 as the record of which files they were. `books/fetch_book.sh` keeps its list, drops the exception clause and states the blanket again in DEC-201's own words: the repository bundles nothing whose licence is unstated. Both are worded without the token the accepts greps for, which is why neither names the files; DEC-201 does. **Gate**: 37/37 fast in `build` (85.09 s) and 37/37 in `build-tune` (85.07 s), `./clang-format.sh --check` clean under `CLANG_FORMAT_MAJOR=22`, whole chain exit 0. `plan_prose_check.py --citations --touches --prose` each exit 0. No `src/` file was touched. **DEV_MANUAL.md and MANUAL.md need no change** (DOCS, checked): `MANUAL.md` never mentions the GUI, and `DEV_MANUAL.md`'s only two hits for the word are the adjective in unrelated sentences; neither documents an audio dependency. **Three things deliberately not done.** The SDL_mixer linkage stays: it is the pixello submodule's, its `init()` calls `Mix_OpenAudio` whether or not the application loads a clip, and editing a third-party submodule is outside `excludes`. `README.md`'s `libsdl2-mixer-dev` line therefore stays true, and it is the owner's file in any case. Stale byte copies of the seven files sat in eight gitignored build trees from old configures; they were removed with `rm -rf build*/tests/assets/gui/sound`, nothing tracked and nothing any build needs.

## Why this exists

S211 licensed the pieces, sourced or deleted the images, and inventoried the
directory; the seven sounds were the one thing left whose origin nobody can
state -- committed 2025-03-09 in `766465f` with no author or copyright
anywhere. DEC-201 deletes them rather than disclose a gap that has no rights
holder to disclose. Filler behind S151: tests only, no run, no `src/`.

## Cost

Agent work, under an hour. The GUI build is the proof; the fast suite does not
build the GUI, so the stamp states the GUI build and the five-second run.
